# Fuse V3.1 Plan: sharpen the open-ended floor, offline and local

The V3 wave (R0 through R9, archived in [V3-Plan.md](V3-Plan.md)) built the semantic moat, hybrid retrieval, abstention, a wider analyzer set, warm latency, peer and agent runs, and a task-resolution harness. It also surfaced, honestly, the one mode where Fuse is weak: open-ended discovery from a vague description. This plan attacks that, plus the freshness limit that gates graph-aware discovery, plus the broader ambition: to beat the field on the axes Fuse can own (token-efficient context, change impact, .NET wiring, agent-loop recall) and reach parity elsewhere.

Policy change in this plan: the old "no model download" floor is retired. A small local embedding model is now bundled and on by default (the dense channel ships in the box), and it runs entirely offline at query time. Two rules still hold: no model rewrites or paraphrases the caller's query (the agent explores freely; the engine bridges the vocabulary gap with retrieval, not a generated rephrase), and the deterministic lexical path remains as a graceful fallback when the bundled model is genuinely unavailable. So "offline and local" stays; "no model present at all" is no longer the design floor.

This file is the forward plan. Each item carries its engine work plus the tests, docs, and benchmark changes that land with it. Tick a box when its definition of done is met; record the measured result in `tests/benchmarks/results` and resync `AGENTS.md` and the benchmarks page in the same change. Plain ASCII prose only.

## Strategy decision: refuse to guess, hand back a map

Fuse is an MCP server in a loop with a model, so it does not have to win the vague one-shot. The primary answer to a low-signal request is not to return a low-precision list, it is to refuse-and-route: tell the caller the request lacks a usable anchor, hand back the partial structure Fuse did see (candidate concepts, entry points, namespaces, nearest symbols), and ask for a symbol, route, service, config, git base, or a narrower description. The calling model, which is exploring the code anyway, then issues a better-anchored query. This converts the weakest mode from an engine-recall problem into a protocol contract, and it takes the hardest input (genuinely no-signal titles) off the table by design.

Three rules keep the contract helpful rather than a wall:

- Refuse-and-route, never just refuse. The response must include what matched (top candidate areas and symbols), so the refusal is a navigation step, not a closed door. An agent that is exploring does not yet know the right symbol; the map is how it finds the next probe.
- Graded, not binary. Three states: confident (return the tight set), partial (return a small best-effort set, flagged low-confidence, with refinement options), insufficient (refuse-and-route). A hard refusal on a borderline-but-answerable query is the new way to be wrong, so the classifier that draws these lines is what must be measured.
- Strict is opt-in with a graceful default. Default to best-effort-plus-flag so a client that cannot refine still gets something; offer a strict mode that hard-requires an anchor for cooperative clients (an agent loop). Do not break callers that send one query and cannot re-prompt.

What this does not remove: once a good, code-anchored query arrives, the engine must be excellent. The contract removes the vague-input burden, not the quality burden. So the deterministic quality work below stays; what is demoted is recall-chasing aimed only at no-signal input.

## Where V3.1 starts (recorded V3 baseline)

All from `tests/benchmarks/results`, counted with `o200k_base`.

- Open-ended localization (Suite C, title only, no base, 108 PRs): overall changed-file recall 30.3 percent lexical (R1) and 34.6 percent with the local dense channel (R2). By bucket: identifier-rich 41 percent (still short of the retired 49 percent lexical floor), natural-language domain 27 percent lexical and 34 percent with dense, formatting 8 percent, route/API low, test-only about 34 percent.
- Precision on the open-ended path is 7 to 8 percent: the engine returns a fixed candidate count regardless of confidence, so most returned files are noise.
- Low-signal detection (R3) is F1 1.0: it correctly abstains on merge, dependency, and CI titles. This must not regress.
- Freshness: incremental re-index (R7) refreshes a changed file's syntax rows (symbols, chunks, full-text, routes) but not the cross-file semantic edges (DI, route, MediatR, EF), which only a full re-index recomputes.
- Agent loop (Suite D): the Fuse MCP tools edge out native file tools on recall at parity tokens; the win concentrates on tasks whose description names wiring.

The root cause of the open-ended weakness: a prose description ("discount rounding is off at checkout") shares almost no tokens with the code (`PriceCalculator`, `ApplyDiscount`, `decimal`, `MidpointRounding`). The default FTS tokenizer (`unicode61`) treats `ApplyRoundingMode` as one opaque token, so "rounding" does not match it. Everything below closes that gap deterministically, then makes the result precise, fresh, and good to explore iteratively.

## The crown for V3.1 (measurable target per axis)

| Axis | Today | V3.1 target |
|------|-------|-------------|
| No-signal handling (the contract) | returns low-precision junk | Refuse-and-route on no-signal input: correct-refusal rate at or above 0.90 with a false-rejection rate on answerable queries at or below 0.10, and the refusal carries navigation options |
| Code-anchored recall (Suite C, refined query) | not measured separately | This is the primary recall measure. Beat the retired 49 percent floor on identifier-rich; lift natural-language meaningfully, all offline |
| Open-ended precision (Suite C, when it does answer) | 7 to 8 percent | 20 percent or more at the reported recall, via the graded cutoff |
| Multi-file recall | partial even when the main file is found | Co-change and graph expansion recover sibling files; report per-PR file completeness |
| Freshness | syntax rows only on incremental | Edited-file semantic edges current after a bounded incremental re-index, in under 1 second for a single-file edit |
| Iterative exploration (Suite D) | slight edge at parity tokens | Higher recall at fewer tool calls and tokens than native, via fresh structural navigation primitives |
| Dense by default | dense is opt-in, off without a model | A small embedding model bundled and on by default, offline at query time; lexical is the fallback, not the headline |
| Multi-language readiness | C# and Roslyn only, hardwired | A load-bearing language-provider seam, a language tag through the index, every V3.1 feature language-agnostic, and a second-language syntax-tier spike indexing and localizing end to end, with C# numbers unchanged |
| Beat the field (new) | one peer (CodeGraph), 6 PRs, home turf | A scaled head-to-head harness including coa-codesearch and Serena on a semantic-capable corpus, winning tokens-to-context and agent-loop recall and reaching latency parity, reported apples-to-apples |
| Cold start (new) | about 64 seconds first index | Syntax-tier context served in a few seconds, the semantic graph upgraded in the background, warm calls unchanged |

Note the deliberate omission: "high recall from a bare vague title" is no longer a target. The contract rejects such input and routes it to a better query, so we measure the refusal quality and the code-anchored recall instead.

## Execution checklist

Wave 1 (landed, see the progress log):

- [x] S3 The MCP signal-sufficiency contract: refuse-and-route on low signal, graded, graceful default plus strict mode (the centerpiece)
- [x] S1 Subword identifier indexing (split CamelCase, snake_case, digits at index and query time)
- [x] S2 Stemming and a weighted comment/doc bridge field in the full-text index
- [~] S5 Structural priors: dependency centrality landed; git co-change deferred (no collector, see A6)
- [x] S7 Graph-aware discovery: expand weak seeds through the semantic graph (opt-in)
- [x] S8 Iterative exploration primitives (neighborhood, callers, central files)
- [~] S10 Multi-language foundation: syntax-tier provider seam and a Python spike landed; language tag and semantic-tier seam remain (see A8)

Wave 2 (the ambition wave: beat the field, plus the carried-over recall and freshness work). Every item below lands engine plus tests plus website docs plus a benchmark in one change:

- [x] A1 Dense by default: bundle a small local embedding model, retire the no-model floor, dense on by default with lexical as fallback
- [x] A2 Peer benchmark harness at scale: install and run coa-codesearch and Serena head-to-head on multiple axes
- [x] A3 Corpus rebuild for semantic mode: buildable repos that restore on .NET 10, regenerated PR ground truth
- [~] A4 Cold-start fix: syntax-first serve, background semantic upgrade, and parallel extraction landed; the resident Roslyn workspace reuse is deferred (see progress log)
- [ ] S6 Dependency-scoped incremental re-index so edited-file semantic edges stay fresh (built on A4's resident workspace) - deferred with the resident workspace
- [x] A6 Git co-change collector and prior (the deferred S5 half): a bounded git-history miner, then the blend
- [ ] A5 Tree-sitter multi-language breadth: a tree-sitter-backed syntax provider for the top languages
- [ ] S10b Multi-language tag plus semantic-tier seam: the language tag column and the typed-graph provider seam
- [ ] S4 Codebase-derived synonym and abbreviation thesaurus (only if anchored-query numbers still lag)
- [ ] S9 Offline learning-to-rank for the fusion weights, plus a richer-query eval mode

Positioning and go-to-market (P-series, a parallel track, not gated on the engineering order):

- [x] P1 Messaging source of truth: the simple pitch, the message hierarchy, the surface-versus-depth layering rule, the honesty pillar
- [x] P2 Landing page rewrite (simple, outcome-first: efficiency and speed, one demo, install in 30 seconds)
- [x] P3 README rewrite (developer first 60 seconds: install, MCP config, quickstart, the tool verbs, honest benchmarks, status)
- [ ] P4 Demo asset and launch post (a real before-and-after agent session on eShopOnWeb, plus a launch blog post)
- [x] P5 Honest comparison table, gated on A2 and A3 so every claim is reproducible

---

## S1. Subword identifier indexing (the foundation)

The single biggest offline lever. The FTS index must contain the subwords of every identifier so a prose word matches a compound name. `ApplyRoundingMode` must be findable by `apply`, `rounding`, and `mode`.

- Work: at index time, split each identifier (in the chunk name, declared symbols, and signature, and optionally body identifiers) on CamelCase humps, `snake_case`, and letter/digit boundaries, and write the expansion into a dedicated FTS column (for example `subtokens`) alongside the original. Keep the original token too so exact matches still win. Apply the same splitting to the query before it hits FTS (the lexical generator already splits CamelCase for ranking; extend that to the FTS match expression and add the new column to the `bm25()` weighting, weighted below the exact name but above the body). No tokenizer plugin is required: the expansion is computed in C# and stored as text, so it works on any SQLite with FTS5.
- Tests: a query for a single subword (`rounding`) returns a file whose only match is a compound identifier (`ApplyRoundingMode`); an exact full-identifier query still ranks the exact declaration first; the subtokens column is populated at index time and survives store disposal; snake_case and digit boundaries split.
- Docs: `internals/scoping-internals.mdx` (the subword field and its weight), `internals/caching-internals.mdx` if the schema version bumps.
- Benchmark: Suite C by bucket, expecting identifier-rich to recover past the 49 percent floor and natural-language to gain, no-model. Report precision alongside (subword expansion widens recall, so watch the trade and let S3 tighten it).

## S2. Stemming and a comment/doc bridge field

Two cheap, purely-offline vocabulary bridges that compose with S1.

- Work: (1) Stemming. Switch the full-text tokenizer for the prose-bearing columns to the built-in `porter` stemmer wrapping `unicode61`, and stem the query terms to match, so "rounding", "rounds", and "rounded" collapse. Keep an unstemmed path for exact identifier matches so stemming does not corrupt code tokens. (2) Comment bridge. Index XML doc comments and inline comments as their own weighted FTS field (the `FUSE_FIELDED_COMMENTS` lever exists but is off and unmeasured here): human-written prose in comments uses the developer's vocabulary, which is exactly the vocabulary a vague query is written in. Weight comments above body but below names.
- Tests: a stemmed query ("calculate") matches a file whose code says "Calculation"; a term that appears only in a doc comment localizes that file; exact identifier matching is unaffected by stemming (a regression guard).
- Docs: `internals/scoping-internals.mdx`, `reference/configuration-keys.mdx` (comment field now on by default or documented), the stemming note in scoping.
- Benchmark: Suite C natural-language-domain recall (the bucket comments help most), no-model.

## S3. The MCP signal-sufficiency contract (the centerpiece)

Because Fuse is an MCP server in a loop with a model, the answer to a vague request is to refuse-and-route, not to guess. This is the highest-leverage item: it removes the worst input from scope and fixes the precision disaster (7 to 8 percent) at the same time.

- Work: turn the localize and search path into a graded contract with three outcomes computed deterministically from the candidate score distribution and the existing `QuerySignalClassifier` (R3), no model:
  - Confident: a candidate (or a tight cluster) stands clear of the rest. Return that tight set; this is where precision is won.
  - Partial: there is some signal but no clear winner. Return a small best-effort set, marked low-confidence, together with refinement options (the top candidate areas and symbols) and the inputs that would sharpen it.
  - Insufficient: no usable anchor (the R3 no-signal classes, or a flat, near-uniform score distribution). Refuse-and-route: return no file list, and instead the map Fuse did see (candidate concepts, entry-point files, namespaces, nearest symbols) plus an explicit ask for a symbol, route, service, request, config section, git base, or a narrower description.
  Add a strictness policy: default is best-effort-plus-flag (a non-cooperative client still gets the partial set), and a strict mode (an `--strict`/option, off by default) hard-requires an anchor and returns only the refuse-and-route payload on insufficient input. The refuse-and-route payload is a typed response shape, not a bare error string, so a calling agent can parse the options and issue the next query. Any change to the MCP response DTO honors the AGENTS.md change-safety invariants (bump the protocol version and update the contract test).
- Tests: a query with a clear winner returns the confident tight set (precision up); a flat score distribution returns the partial set with refinement options; a no-signal title returns the insufficient refuse-and-route payload carrying candidate areas (not an empty error); strict mode refuses an unanchored query and answers an anchored one; the default mode never returns nothing for a client that cannot refine; the graded thresholds are reproducible for fixed scores; low-signal classification F1 holds at or above 0.80 and the false-rejection rate on answerable queries stays at or below 0.10.
- Docs: `concepts/scoping.mdx` (the three states and why refuse beats guess), `reference/mcp-tools.mdx` (the graded response shape and the strict option), `scenarios/ask-one-question.mdx` (the refine-and-retry loop).
- Benchmark: Suite C reframed: correct-refusal rate on the no-signal bucket, false-rejection rate on the answerable buckets, and precision at the reported recall when it does answer. This replaces "recall from a bare vague title" as the headline for this path.

## S4. Codebase-derived synonym and abbreviation thesaurus (demoted)

Demoted by the S3 contract: we no longer need to rescue no-signal input, so the thesaurus is justified only if it measurably helps code-anchored queries (an anchored query using an abbreviation the repo expands). Land it only after S1, S2, and S5 show a residual vocabulary gap on anchored queries; otherwise defer. Bridge vocabulary the codebase itself defines, learned offline from the repo, no model.

- Work: at index time, build a per-repo thesaurus from two signals: (1) abbreviation rules over the repo's own identifiers (btn to button, calc to calculate, msg to message, amt to amount, idx to index, repo to repository), derived by matching short identifiers against their longer co-occurring forms; (2) a distributional thesaurus over identifier and comment co-occurrence (the `DistributionalThesaurus` in `Fuse.Fusion.Scoping` exists from the V2 lexical era and is unplugged), so terms that appear in the same chunks become weak synonyms. Persist the thesaurus alongside the index. At query time, expand the query with the top-weighted synonyms (a bounded, low-weight expansion, like pseudo-relevance feedback but from the learned map rather than the top hits). Deterministic and offline.
- Tests: a known abbreviation expands to its full form and localizes the file; the thesaurus is built deterministically and persisted across disposal; expansion is bounded so it cannot flood the candidate pool.
- Docs: `internals/scoping-internals.mdx` (the learned thesaurus), `reference/configuration-keys.mdx` (any lever).
- Benchmark: Suite C, expecting the natural-language and identifier buckets to gain where the repo's naming is consistent; report the ceiling honestly (thesaurus quality is bounded by the repo's own naming discipline).

## S5. Structural priors: co-change and centrality

Use the structure Fuse already records to rank ambiguous candidates, no model.

- Work: blend two priors into the candidate score through the existing noisy-or scorer. (1) Git co-change: the `git_cochange` table (path, count, pmi, jaccard) is collected but not used on the open-ended path; boost a candidate when it co-changes with a stronger candidate, which recovers the sibling files of a multi-file change. (2) Dependency centrality: a degree or PageRank prior over the semantic graph (the `GraphCentrality` helper exists, unplugged), so a central, widely-depended-on file outranks a leaf for an otherwise-tied query. Both priors are small multipliers, capped so they tune rather than dominate. The `FUSE_GIT_CHURN_WEIGHT` lever shows the shape; co-change and centrality are the recall-oriented additions.
- Tests: a co-changing sibling of a strong hit is boosted into the returned set; a central file outranks a leaf for an ambiguous query; the priors are bounded (a high prior cannot by itself promote an irrelevant file).
- Docs: `internals/scoping-internals.mdx` (the priors and their caps), `reference/configuration-keys.mdx`.
- Benchmark: Suite C multi-file recall (per-PR file completeness, not just any-hit), and Suite B blast-radius precision.

## S6. Dependency-scoped incremental re-index (freshness)

The freshness fix. Today incremental refreshes only a changed file's own rows; the cross-file semantic edges go stale until a full re-index. That gates S7 (graph-aware discovery) and degrades the iterative-exploration story exactly when an agent is editing.

- Work: when a file changes, re-analyze that file plus the bounded set of files that reference its declared types (its project and direct dependents), and recompute only the affected semantic edges (DI, route, MediatR, EF, hosted, pipeline, decorator, factory, endpoint), leaving the rest of the graph intact. The semantic edges are cross-file, so the changed file's compilation neighborhood must be re-evaluated, but it is bounded, not the whole solution. Optionally, debounce a background full re-index after a burst of edits settles, serving the slightly-stale graph in the meantime with a freshness marker the caller can read. Honor the AGENTS.md change-safety invariants.
- Tests: editing a file to add a DI registration makes the new resolve edge appear after the incremental call (not just the symbol rows); an unrelated edge elsewhere is unchanged; the re-analyzed set is bounded to the dependent neighborhood (an invariant test, not the whole graph); a deleted file's edges are removed.
- Docs: `project/performance.mdx` (replace the current "syntax rows only" caveat with the dependency-scoped semantics and a freshness number), `internals/pipeline.mdx`.
- Benchmark: the `performance` suite gains a freshness measurement (single-file incremental that updates edges, time under 1 second) and a correctness check that the post-incremental graph matches a full re-index on the changed neighborhood.

## S7. Graph-aware discovery

Now that the graph is fresh (S6), let a weak text or dense seed inherit its neighborhood, so a vague query that barely hits one file pulls in its callers, implementers, and configuration.

- Work: after candidate generation and scoring, expand the top one or two seeds through the typed semantic graph to a shallow depth, admit the neighbors at a decayed score, and re-rank the enlarged set (reuse the `GraphExpansionEngine` already used by review and context, applied to the localize path). Bound the expansion so a single weak seed cannot pull in a large subtree. This is where the moat starts helping discovery, not only resolution.
- Tests: a query that lexically hits only an interface returns its implementation and a caller through expansion; expansion is bounded by depth and score; with no semantic graph (syntax mode) the path degrades to lexical without error.
- Docs: `concepts/scoping.mdx`, `internals/scoping-internals.mdx` (expansion on the discovery path).
- Benchmark: Suite C recall, especially on .NET-wiring-shaped queries; confirm precision does not collapse (the cutoff from S3 and the bounded expansion keep it in check).

## S8. Iterative exploration primitives

Accept that agents explore rather than ask one clean question. Give them cheap, fresh, structural navigation so a weak first guess becomes a strong few-call funnel.

- Work: expose first-class, deterministic operations an agent calls iteratively: the neighborhood of a set of files (graph neighbors plus co-changers plus same-folder cohesion), the callers and implementers of a symbol, and the structurally central files of an area, each returned ranked and bounded with provenance and no bodies. These compose the priors (S5), the fresh graph (S6), and expansion (S7) into tools shaped for exploration. Keep them warm and fast (tens of milliseconds) so an agent can chain several cheaply.
- Tests: neighborhood expansion returns callers, implementers, and co-changers of a seed, ranked and bounded; central-files returns the high-degree files of a folder; each operation is body-free and provenance-tagged.
- Docs: `reference/mcp-tools.mdx` (the exploration tools), `scenarios` (an iterative exploration walkthrough).
- Benchmark: Suite D (agent), measuring whether the funnel reaches higher file recall at fewer tool calls and fewer tokens than native exploration, on the wiring-shaped and the prose-shaped tasks.

## S9. Offline learning-to-rank and a richer-query eval

Tune the now-richer feature set without a query-time model, and measure the realistic case. With the S3 contract in place, the richer-query (code-anchored) regime is the primary measure and the bare-title regime is the refusal test, so this item's eval half is also what validates the contract's framing. The learning-to-rank half is for anchored queries; it is not aimed at rescuing vague input.

- Work: (1) Learning-to-rank. Train a small linear or gradient-boosted ranker on the PR ground truth, with features per candidate (exact-name match, subword match, stemmed match, dense similarity, path match, co-change prior, centrality prior, signal bucket), and ship the learned coefficients. Scoring at query time stays deterministic arithmetic over the features, no model loaded; the learning is offline and the result is a static weight table. This replaces or feeds the hand-tuned noisy-or weights. (2) Richer-query eval. The benchmark feeds bare PR titles, the worst case. Add a deterministic richer-query mode that synthesizes a developer-style query per PR from the diff (the changed symbol names plus a one-line description extracted from the diff, no model), so we measure the realistic exploration input as well as the title-only floor, and report both.
- Tests: the learned weights reproduce or beat the hand-tuned baseline on a held-out slice; scoring from coefficients is deterministic; the richer-query generator is deterministic and produces a query distinct from the title.
- Docs: `internals/scoping-internals.mdx` (the learned weights and how they are trained offline), `project/benchmarks.mdx` (the two query regimes).
- Benchmark: Suite C overall and by bucket under both the title-only and the richer-query regimes, no-model.

## S10. Multi-language foundation (make the seam load-bearing)

Today the moat is C# and Roslyn only. This item does not add a second language; it makes the architecture ready for one and proves the seam with a syntax-tier spike, so a later language drops in without reworking the engine or the V3.1 retrieval features. The plugin projects already exist (`Fuse.Plugins.Abstractions`, `Fuse.Plugins.Languages.CSharp`, `Fuse.Plugins.Languages.CSharp.Roslyn`, `Fuse.Plugins.Formats.Web`); the work is to make the indexer and retrieval consume that seam generically rather than hardwiring Roslyn, and to add the spike.

- Work:
  - Define a language-provider capability surface in `Fuse.Plugins.Abstractions`: discovery (the file extensions a provider claims), a syntax-tier extractor (symbols and chunks from a file, no compiler), and an optional semantic-tier extractor (a typed-graph builder backed by a compiler or language server). Declare a capability level per provider (syntax-only, or semantic) so the indexer records the achieved level the same way it records semantic, partial, and syntax today.
  - Refactor `SemanticIndexer` to select providers by file extension and call the generic seam, with the existing Roslyn path becoming the C# provider behind that seam (no behavior change for C#). The MSBuild and Roslyn loaders stay inside the C# semantic provider, not in the shared indexer.
  - Add a `language` tag to the index: a column on files, symbols, and graph nodes, and include the language in node ids where collisions are possible, so a multi-language graph and retrieval can filter or blend by language. Bump the schema version per the change-safety invariants.
  - Namespace the wiring analyzers by language: the C# DI, route, MediatR, EF, and endpoint analyzers register under the C# provider, and the seam lets a future provider register its own wiring detectors (for example a Python web-framework route analyzer) without touching the shared runner.
  - Keep every V3.1 retrieval feature language-agnostic by construction: the subword indexing (S1), stemming and comment field (S2), co-change and centrality priors (S5), graph-aware discovery (S7), exploration primitives (S8), and the signal-sufficiency contract (S3) all operate over the generic symbol, chunk, and edge tables, so they carry to a new language for free. This item verifies that and fixes any place a feature reached into the C# path directly.
  - Land a second-language spike at the syntax tier only: a minimal provider for one additional language (for example TypeScript or Python) that extracts symbols and chunks (tree-sitter or a bounded regex lexer, offline, no language server), enough to prove the pipeline indexes, full-text-searches, and localizes a non-C# file end to end. The semantic graph for that language is explicitly out of scope here; the point is the seam, not the second moat.
- Tests: a fake or spike provider registered alongside C# is selected by extension and indexes a non-C# fixture to symbols and chunks; the achieved capability level is recorded; a localize over a mixed-language fixture returns files from both languages; the `language` tag is set on files, symbols, and nodes; the C# golden outputs and Suite A are unchanged after the refactor (a no-regression guard); one V3.1 retrieval feature (for example subword search) is shown to work over the spike language without C#-specific code.
- Docs: a new internals page on the language-provider architecture and how to add a language (the capability surface, the syntax versus semantic tiers, where wiring analyzers register), and a note in `internals/pipeline.mdx` that the indexer is provider-driven. Update `AGENTS.md` design invariants to state the seam.
- Benchmark: a no-regression run of Suite A and the C# suites confirming identical numbers after the refactor, plus a small spike-language fixture scored for symbol and chunk extraction and a localize recall sanity check, recorded as the multi-language readiness baseline (not a full semantic benchmark for the new language).

---

## Wave 2 items (the ambition wave: beat the field)

These items extend V3.1 toward the explicit goal of beating the peer field on the axes Fuse can own and reaching parity elsewhere. Each lands engine plus tests plus website docs plus a benchmark in one change, runs the three gates, and follows every convention below. Website docs live under `site/content/docs`.

### A1. Dense by default (retire the no-model floor)

The single biggest recall lever already exists (the R2 dense channel) but is off without a model. Ship it on.

- Work: bundle a small quantized local embedding model (for example a MiniLM or bge-small ONNX) with the tool, or fetch-once-and-cache on first run, and resolve it without `FUSE_DENSE` so the dense channel is on by default. Keep query time fully offline (no network call). The deterministic lexical path stays as the fallback when the model is genuinely absent or fails to load. The no-rewrite rule is unchanged: the query string is embedded, never paraphrased by a model.
- Tests: the dense channel is active by default; lexical fallback engages when the model is absent (a regression guard); query time makes no network call; the embedded text is the raw query, not a rewrite.
- Docs: `concepts/scoping.mdx` (dense on by default), `reference/configuration-keys.mdx` (the bundled model and how to disable it), `internals/scoping-internals.mdx` (the dense channel and the retired floor).
- Benchmark: Suite C with dense on by default versus the lexical fallback, expecting the natural-language bucket to gain; record the model size and load time.

### A2. Peer benchmark harness at scale (so the claim can be made)

You cannot claim to beat a field you have not run. This makes the claim falsifiable.

- Work: extend the layer-6 peer harness to install and run coa-codesearch (build the `COA.CodeSearch.McpServer` host) and Serena alongside CodeGraph and Fuse; scale well past 6 PRs; score multiple axes at the same budget and ground truth (tokens-to-context, changed-file recall at a fixed budget, cold and warm latency, and task-resolution where a build/test oracle exists). Omit a peer only when it genuinely cannot be installed, and record why.
- Tests: each peer arm runs and records a result; an absent peer is reported as omitted, never stubbed or scored zero; scoring is deterministic for fixed inputs.
- Docs: `project/benchmarks.mdx` (the peer methodology and the head-to-head table), `AGENTS.md` measured results.
- Benchmark: the head-to-head results, recorded under `tests/benchmarks/results`.

### A3. Corpus rebuild for semantic mode

The corpus loads mostly syntax (the pinned commits fail to restore from central-package-management and SDK drift), which hides the moat in our own numbers. The build SDK here is .NET 10 (`global.json` pins `10.0.100`).

- Work: repin or replace the corpus with repositories that restore and build on .NET 10 so they index in semantic or partial mode. Prefer small, low-dependency, well-maintained libraries plus at least one application; a verified clean candidate is Scrutor (builds on net8.0 and net10.0 with zero warnings), and similar clean libraries (for example Polly, Ardalis.Specification, NodaTime) and a restorable ASP.NET application. Regenerate the PR ground truth (`prs.json`) from each repo's merge history at the new commits, keeping the misleading-maintenance-title drop rule. Record the achieved index mode per repo and target semantic or partial as the norm.
- Tests: the corpus manifest loads; each pinned repo restores and indexes at or above partial here (or is marked syntax-only with a reason); the regenerated `prs.json` parses.
- Docs: `project/benchmarks.mdx` (the corpus table and reproduction), `AGENTS.md` corpus description.
- Benchmark: re-run Suites A through C on the rebuilt corpus and record the new index-mode distribution (the graph-dependent items S5, S7, S8 should now move where they were flat).

### A4. Cold-start fix (syntax-first, semantic in background)

The first index takes about 64 seconds (it includes the MSBuild load). Warm is already milliseconds.

- Work: serve syntax-tier context within a few seconds of a first call, then compute the semantic graph in the background and swap it in with a freshness or mode marker the caller can read; hold a resident Roslyn workspace in the warm host and reuse the MSBuild evaluation across calls; parallelize per-file extraction. Warm-call results are unchanged.
- Tests: a first call returns syntax-tier results without waiting for the full semantic load; the background upgrade replaces the graph and flips the marker; warm results are unchanged; parallel extraction is deterministic.
- Docs: `project/performance.mdx` (the cold-start path and numbers), `internals/pipeline.mdx` (syntax-first then background semantic).
- Benchmark: the performance suite gains a cold-start measurement (time to first syntax-tier answer, time to semantic-ready) on a real project.

### A6. Git co-change collector and prior (the deferred S5 half)

The `git_cochange` table has no collector; build it, then blend.

- Work: a bounded git-history co-change miner: parse `git log --name-only` over a bounded commit window (honoring the bounded external-process-args invariant: chunk the list or pass via stdin), count file pairs that change together (skip wide commits such as merges and sweeps to bound the pairwise blowup), compute count, PMI, and jaccard, and upsert into `git_cochange`; gate it with a commit cap and a per-commit file cap so it does not balloon the cold index. Then blend the co-change prior into the open-ended scorer (boost a candidate that co-changes with a strong candidate).
- Tests: the miner populates `git_cochange` from a small fixture repo deterministically; wide commits are skipped; the external-process args are bounded (an invariant test); a co-changing sibling of a strong hit is boosted in; the prior is bounded so it cannot alone promote an irrelevant file.
- Docs: `internals/scoping-internals.mdx` (the co-change prior), `internals/caching-internals.mdx` (the collection and the schema), `reference/configuration-keys.mdx` (the window and caps).
- Benchmark: Suite C multi-file recall (per-PR file completeness) and the cold-index time delta from mining.

### A5. Tree-sitter multi-language breadth

Reach coverage parity with language-agnostic peers so they cannot win on breadth.

- Work: add a tree-sitter-backed `ILanguageSyntaxProvider` for the top languages (for example TypeScript or JavaScript, and Python beyond the regex spike) so non-C# repositories get symbol and chunk extraction, token-efficient context, and localization, offline and with no language server. Register through the S10 seam. The semantic typed graph for these languages is out of scope (syntax tier only).
- Tests: a tree-sitter provider extracts symbols and chunks from a fixture per added language; a mixed-language workspace indexes and localizes files from all of them; the C# numbers are unchanged.
- Docs: a new internals page on adding a language (the seam and the tree-sitter providers), a `reference/mcp-tools.mdx` note that retrieval is language-agnostic.
- Benchmark: a small multi-language fixture scored for symbol and chunk extraction and a localize sanity check per language, recorded as the multi-language breadth baseline.

### S10b. Multi-language tag and semantic-tier seam (the deferred S10 half)

- Work: add a `language` tag column to files, symbols, and nodes (schema bump per the change-safety invariants) populated from the selecting provider; define the semantic-tier provider capability so a language can supply a typed-graph builder (move the MSBuild and Roslyn loaders behind a C# semantic provider with no behavior change); namespace the wiring analyzers by language so a future provider registers its own detectors.
- Tests: the language tag is set on files, symbols, and nodes; a localize over a mixed-language fixture can filter or blend by language; the C# golden outputs and Suite A are unchanged after the refactor.
- Docs: the internals language-provider page (the semantic tier and the tag), `AGENTS.md` design invariants.
- Benchmark: a no-regression run of Suite A and the C# suites confirming identical numbers after the refactor.

---

## Positioning and go-to-market (P-series)

A parallel track to the engineering. The goal is to sell the tool clearly to a skeptical .NET audience without overclaiming. Two framing rules govern every public surface:

- Surface versus depth (the layering rule). Landing, overview, and other top-of-funnel pages stay abstract and simple: lead with the outcome a developer feels (their AI assistant gets faster, cheaper, and more accurate on their codebase), not the mechanism. Most developers do not want DI-graph-resolution theory up front; they want "it makes my agent better and uses fewer tokens." The deeper concepts (deterministic wiring resolution, the semantic graph, the signal-sufficiency contract) live in the concept, reference, and internals pages for the readers who go looking. This matches the existing writing rule (user-facing pages are outcome-first; reference pages stay dense).
- Honesty is the brand. Every number is sourced to `tests/benchmarks/results`; weaknesses are published alongside strengths; no head-to-head "we beat X" claim ships before A2 and A3 measure it apples-to-apples. The refuse-and-route behavior is part of the trust story (when Fuse cannot answer well it says so and hands back a map, rather than guessing).

The .NET-first wedge is the substance underneath the simple surface: be the clear best at .NET agent context, present other languages as broad syntax-tier coverage with the deepest support on .NET. The surface message is the benefit (faster, cheaper, more accurate); the wedge is how it is true.

Acceptance for every P item: the site builds and lints clean (`site` is Next.js plus Fumadocs), prose is plain ASCII, and any number shown is quoted from `tests/benchmarks/results` (no fabricated or rounded-up figures).

### P1. Messaging source of truth

- Work: write one short internal messaging doc that the site and README both draw from: the one-sentence pitch, the simple top-of-funnel benefit statements (efficiency, speed, accuracy), the message hierarchy (what leads, what is demoted: raw token-reduction percentages are demoted), the surface-versus-depth layering rule, the differentiation framing (versus grep and RAG, whole-repo packers, tree-sitter graph tools, and code-search servers, stated as capability differences not measured wins), and the honesty pillar. This is the reference the other P items implement against, so they stay consistent.
- Docs and acceptance: lives under `site/content/docs` (or a docs/marketing note) so it is versioned with the product; reviewed against the writing rules; no unsourced claims.

### P2. Landing page rewrite

- Work: rewrite the landing and overview pages simple and outcome-first. Hero: the benefit in one line (a faster, cheaper, more accurate AI assistant on your code), with one short "see it" artifact (a before-and-after of an agent answering a real question with and without Fuse). Three plain proof tiles (accurate answers about how the code is wired, a PR's context in roughly 900 tokens, answers in milliseconds once warm), an install-and-wire-up block (the MCP config for Claude Code and Cursor, copy-paste), and a link to the honest benchmarks. Keep deep concepts off the hero; link to them.
- Docs and acceptance: the marketing pages under `site` build and lint clean; plain ASCII; numbers sourced; the deep mechanism is one click away, not in the hero.

### P3. README rewrite

- Work: rewrite the repository README for a developer's first 60 seconds: a one-paragraph what-and-why (the simple benefit, with the wiring sentence as the one concrete hook), the install one-liner and the MCP server config block for Claude Code, Cursor, and a generic MCP client, a six-line quickstart with a real `fuse_resolve` and `fuse_review` example and sample output, the tool surface presented as the verbs an agent uses, a benchmarks section with the source-of-truth honesty note and the weaknesses called out, and a status and contributing section (solid: .NET semantic; early: multi-language).
- Docs and acceptance: README renders correctly on the repo host; plain ASCII; the MCP config snippet is copy-paste correct; numbers sourced.

### P4. Demo asset and launch post

- Work: produce a short (30 to 60 second) real-session demo on a recognizable .NET application (eShopOnWeb, already in the corpus) showing an agent answering a wiring or change-impact question correctly and cheaply with Fuse versus getting lost without it; and a launch blog post that leads with the problem (AI assistants get lost in .NET codebases and burn tokens) rather than the product. Identify the launch channels (the .NET communities, the MCP registry and awesome-mcp lists, a Show HN with the honest-benchmarks angle).
- Docs and acceptance: the asset is linked from the landing page and README; the post's claims are sourced; nothing overclaims a head-to-head win.

### P5. Honest comparison table (gated on A2 and A3)

- Work: once the peer harness (A2) runs on the semantic-capable corpus (A3), publish a reproducible head-to-head comparison (tokens-to-context, recall at a fixed budget, latency) with the exact reproduction command, including the axes where Fuse does not lead. Do not publish any comparison before the harness backs it.
- Docs and acceptance: `project/benchmarks.mdx` carries the table and the `fuse eval` reproduction; every cell is sourced to `tests/benchmarks/results`; omitted peers are marked omitted with a reason, never stubbed.

---

## Sequencing

S3 first: the contract is the centerpiece. It is cheap, it removes the worst input from scope, and it fixes precision, so it should land before the recall work that would otherwise be tuned against input we now reject. Then S1 (subword indexing), the foundation that multiplies every lexical signal on the anchored queries the contract lets through, and S2 (stemming and comments) right after as the next vocabulary bridge. Then S5 (co-change prior, which needs no graph) and S6 (freshness), the prerequisite for S7 (graph-aware discovery) and the strengthener of S8 (exploration). S7 and S8 follow S6. S4 (thesaurus) and the learning-to-rank half of S9 come last and only if the anchored-query numbers still show a gap; the richer-query eval half of S9 should land early enough to measure the contract honestly.

A practical first cut, by leverage and cost: S3, then S1, then S2, then S5 (co-change only), then S6, then S7 and S8, then the S9 eval, then S4 and the S9 ranker only if needed.

S10 (multi-language foundation) runs as a parallel architecture track rather than a strict step in the recall sequence. Its first half (the language-provider seam, the language tag, the indexer refactor with C# behind the seam) should land early, right after S3, so that S1, S2, S5, S7, and S8 are built over the generic symbol, chunk, and edge tables and carry to a new language for free instead of needing a later rewrite. Its second half (the syntax-tier spike for a second language) lands last, once the retrieval features exist, to prove they work on a non-C# language end to end. The hard rule for the whole plan: implement every retrieval feature against the language-agnostic tables, never against the C# path directly, so the foundation stays ready.

### Wave 2 sequencing (the ambition wave)

Wave 1 (S3, S1, S2, S5-centrality, S7, S8, S10-seam) has landed. Wave 2 order, by leverage and by what unblocks measurement:

1. A3 corpus rebuild first. Until the corpus indexes in semantic or partial mode, the graph-dependent items (S5 co-change, S6, S7, S8) cannot be measured and the moat stays invisible in our own numbers. This unblocks every benchmark below.
2. A2 peer harness early. You cannot claim to beat the field without running it; standing the harness up early turns every later item into a measured head-to-head rather than a guess. A3 and A2 together define the scoreboard.
3. A1 dense by default. The biggest single recall lever, already built, just needs to ship on; measure it on the rebuilt corpus.
4. A4 cold-start and S6 freshness together. They share the resident Roslyn workspace, so build them as one infrastructure push: A4 makes the first call fast and serves syntax-first, S6 keeps the semantic edges fresh on incremental edits using the same warm workspace.
5. A6 co-change, then A5 tree-sitter breadth, then S10b (tag and semantic-tier seam), then S4 and S9 only if anchored-query numbers still lag.

The hard rule still holds: implement every retrieval feature against the language-agnostic tables, never the C# path, so each carries to the tree-sitter languages for free.

The P-series (positioning and go-to-market) runs in parallel and is not gated on the engineering order, with one exception: P5 (the comparison table) waits for A2 and A3 so every claim is reproducible. P1 (the messaging source of truth) is worth writing early, since P2 and P3 implement against it; P4 (demo and launch) lands once the product story is stable.

## Honest ceilings

- The contract handles the vague-input ceiling, but it has its own ceilings. Refuse-and-route only helps a client that can act on the refusal; a non-cooperative client gets the graceful best-effort set instead, which is back to the old quality bar, so we cannot fully escape the recall problem for those callers. And the contract introduces a new way to be wrong: a false rejection of a borderline-but-answerable query. The classifier that draws confident, partial, and insufficient apart is therefore the thing to measure and tune (R3 scores the clear no-signal case at F1 1.0; the borderline line is the new work), and the refusal must always carry a navigation map or it just bounces the agent.
- A vague one-shot query still has an information ceiling. Even with the contract, when a client cannot refine, deterministic methods bridge the vocabulary gap and exploit structure but cannot recover a file the query gives no signal for. The realistic target is: refuse-and-route well on no-signal input, beat the lexical floor on identifier-rich anchored queries, lift natural-language meaningfully, raise precision substantially, and turn the free-exploration case into a strong few-call funnel.
- Recall and precision trade on the open-ended path. Subword indexing, stemming, thesaurus expansion, and graph expansion all widen recall and pressure precision; the calibrated cutoff (S3) and the learned ranker (S9) are the counterweights. Report the frontier at more than one operating point rather than claiming both ends.
- Thesaurus and co-change quality are bounded by the repo. A repo with inconsistent naming or a shallow git history gives weaker priors; these are aids, not guarantees, and must be capped so they tune rather than dominate.
- Freshness is bounded by the cross-file nature of semantic edges. A single-file edit can in principle affect distant edges; the dependency-scoped re-index covers the bounded common case (the file's project and direct dependents) and a debounced full re-index covers the rest, with a freshness marker so a stale answer is never presented as fresh.
- Dense is now the default, not a bonus. A small local embedding model is bundled and on by default, offline at query time, so the dense channel is part of the headline path. The deterministic lexical path remains as a graceful fallback when the model is genuinely absent or fails to load, but "no model present at all" is no longer the design floor (the old no-model-download rule is retired). Two rules still hold: query time is offline (no network), and no model rewrites or paraphrases the caller's query.
- "Beat the field" is a measurement claim before it is a feature claim. It is only true once A2 (the peer harness with coa-codesearch and Serena) and A3 (a semantic-capable corpus) exist and report apples-to-apples at the same budget and ground truth. Until then, do not assert a head-to-head win. Realistic target: win tokens-to-context and agent-loop recall decisively, reach latency parity (warm is already milliseconds; A4 fixes cold start), and accept that a pure full-text code search may stay faster on raw cross-language text. Dominate the .NET-structure, change-impact, and token-efficient-context segments; reach coverage parity elsewhere via tree-sitter (A5).

## Reminders and conventions (read before starting a Wave 2 item)

- One item at a time, each as engine plus tests plus website docs (under `site/content/docs`) plus a benchmark in a single change. Run the three gates every item: `dotnet build Fuse.slnx -c Release`, `dotnet test Fuse.slnx -c Release --no-build`, `dotnet format Fuse.slnx --verify-no-changes`. New public API needs XML docs. New tests must actually run (the count rises). Plain ASCII prose only (a stop hook rejects em dashes, smart quotes, and emoji outside code fences).
- Change-safety (AGENTS.md): any `Fuse.Cli.Rpc` DTO or `[JsonRpcMethod]` change bumps `FuseHostService.ProtocolVersion` and `ext/vscode/src/host/protocol.ts` `PROTOCOL_VERSION` together, and updates the extension client and the contract test, in the same change. Any index schema change (A6 git_cochange, S10b language tag) bumps the schema version. Bound external-process arguments (chunk or stdin) for any git invocation (A6). No silent behavior changes. Never fabricate or weaken a benchmark number; quote only numbers recorded in `tests/benchmarks/results`.
- After each item: write results to `tests/benchmarks/results/*.json`, resync `AGENTS.md` measured results and the website docs (benchmarks.mdx plus relevant MDX) in the same change with semantic-engine framing (never "tokens saved" as the headline), tick the box in this file, append a timestamped progress-log entry (Status, Result, Verification, Blockers, Lessons, Time), commit with the item id, push. Keep the single open PR on `feature/v3.1`; never merge, self-approve, or enable auto-merge.
- If an item is genuinely blocked by a missing resource, log it with the concrete reason, leave the box unticked, skip to the next item, and surface it (as was done for the S5 co-change collector and the S10 semantic-tier seam in Wave 1).
- Environment facts: the build SDK is .NET 10 (`global.json` pins `10.0.100`, runtimes 6, 8, 9, 10 installed); the CI vulnerability audit suppresses GHSA-hv8m-jj95-wg3x, GHSA-vh6j-jc39-fggf (MessagePack, unreachable formatter), and GHSA-h4j7-5rxr-p4wc (Microsoft.Build.Tasks.Core, reference-only transitive) in both `Directory.Build.props` and the `ci.yml` audit script; keep those two lists in sync when touching dependencies.

---

## Progress Log

Append a timestamped entry per item as it lands (Status / Result / Verification / Blockers / Lessons / Time), mirroring the V3 history log. The first entry goes here.

### S3 The MCP signal-sufficiency contract (2026-06-27)

- Status: landed. The localize path is now a graded refuse-and-route contract.
- Result: localization grades every request from the candidate score distribution into confident (return the leading cluster), partial (return a capped best-effort set plus a navigation map), or insufficient (refuse and route: return only a navigation map of candidate areas, entry points, and nearest symbols, plus an explicit ask). A `strict` option (CLI `--strict`, MCP `strict`, default off) hard-requires an anchor; the default stays best-effort-plus-flag so a one-shot client is never stranded. New engine types `SignalState`, `NavigationMap`, `SignalGrader` (pure, fixed-threshold) and `NavigationMapBuilder` (over the language-agnostic symbol/route/file tables, no C# path). The R3 `LowSignal` flag stays tied to the no-signal classifier alone, kept distinct from score-distribution insufficiency, so the low-signal detection metric is uncontaminated. No `Fuse.Cli.Rpc` DTO or `[JsonRpcMethod]` change (localize/find are MCP-string tools, not RPC, confirmed by grep over `ext/vscode/src`), so the protocol version is untouched and no schema change.
- Result (measured, `tests/benchmarks/results/localize.s3.json`, 108 PRs, title only, no model): correct-refusal on the no-signal bucket 100 percent (low-signal detection F1 1.0, unchanged), false-rejection on answerable queries 8.1 percent (8 of 99, under the 0.10 target), precision when confident 21.3 percent (18 tasks, over the 20 percent target) against the 6 to 8 percent fixed-count baseline. Graded states confident 18, partial 73, insufficient 17. The cutoff trades recall for precision by design: returned-set recall 16.4 percent at a median 956 returned tokens (from 31.0 percent at 4,642), which S1 subword indexing widens back under this cutoff. Index modes partial 2, syntax 4.
- Verification: `dotnet build Fuse.slnx -c Release` 0 errors; `dotnet test Fuse.slnx -c Release --no-build` green (one Fuse.Fusion SQLite test flaked under parallel load, passes 237/237 in isolation; unrelated to S3); `dotnet format Fuse.slnx --verify-no-changes` clean. New tests: `SignalGraderTests` (8, pure grading) and 5 added `SemanticRetrievalEngineTests` cases (confident tight set, insufficient nav map, no-signal refuse-and-route, strict refuse-vs-answer, default-never-nothing); Fuse.Retrieval.Tests count rose to 60.
- Blockers: none.
- Lessons: the false-rejection error is real but small here because any full-text or dense match clears the insufficient floor (FtsBody 0.55 is well above the 0.30 ceiling), so insufficiency mostly fires on genuinely empty matches; an early eval run read a stale benchmark assembly (the engine had rebuilt but the suite had not, so the new contract notes were missing), fixed by a full `dotnet run` rebuild. Keeping `LowSignal` decoupled from the graded state was necessary to hold the R3 F1 at 1.0.
- Time: one session.

### S1 Subword identifier indexing (2026-06-27)

- Status: landed. Subword expansion of identifiers in a dedicated full-text column at index and query time.
- Result: a new `IdentifierSplitter` (public, in `Fuse.Indexing`) splits any identifier on camelCase humps, snake_case underscores, acronym boundaries, and letter/digit transitions into distinct lowercased subword parts (minimum length 2), language-agnostic by character class. At index time the chunk's name, declared symbols, and signature are expanded into a new `subtokens` FTS5 column (computed in C#, no tokenizer plugin); the bm25 weighting adds the column at 0.9 (below the exact name, above the body). At query time `BuildMatchExpression` expands each query term with its subword parts, so `rounding` matches `ApplyRoundingMode` while the raw term keeps the exact-name match ranking first. Schema version bumped 11 to 12, so the index rebuilds on next run (no incremental migration path in V3). Built over the language-agnostic chunk/FTS tables, not the C# path, so it carries to a future language.
- Result (measured, `tests/benchmarks/results/localize.s1.json`, 108 PRs, title only, no model, vs the S3 baseline): overall returned-set recall 16.4 to 18.3 percent, identifier-rich 27 to 30 percent, natural-language domain 15 to 17 percent, precision 9.7 to 10.7 percent (subwords find the right file, not only more files), false-rejection on answerable queries 8.1 to 7.1 percent. Precision when confident 19.4 percent (from 21.3, within noise on 18 tasks at the widened recall). Identifier-rich stays short of the retired 49 percent floor, bounded by the S3 cutoff and the mostly-syntax corpus (partial 2, syntax 4), reported straight.
- Verification: `dotnet build Fuse.slnx -c Release` 0 errors; targeted `dotnet test` green (new `IdentifierSplitterTests`, 8, and 3 added `WorkspaceIndexFtsTests` for single-subword match, exact-name-still-first, snake_case and digit subwords; Fuse.Indexing.Tests count rose to 32); full suite and format run before commit.
- Blockers: none.
- Lessons: subword expansion raised precision as well as recall here, because the extra matches are the right files, not noise, which is the opposite of the naive expectation that widening recall must cost precision; the S3 graded cutoff is doing the precision protection the plan intended. The 49 percent floor is not comparable to a post-cutoff number, so the honest comparison is S1 against the S3 baseline on the same branch.
- Time: one session.

### S2 Stemming and a comment bridge (2026-06-27)

- Status: landed. Two offline vocabulary bridges composing with the S1 subwords.
- Result: a compact `PorterStemmer` (the classic 1980 algorithm, in `Fuse.Indexing`) stems only all-letter words of three or more characters, so prose collapses to stems while identifiers, numbers, and short tokens pass through. A new `stems` FTS column holds the stemmed expansion of each chunk's identifiers and comments (weighted 0.5, the lowest, a fuzzy bridge); query terms are stemmed the same way, so `calculate` finds `Calculation`. A `CommentExtractor` (lexical scan for line, block, XML-doc, and hash comments, markers and tags stripped) populates the chunk `comments` field in syntax mode, so the developer's comment vocabulary is indexed above body weight. Schema version 12 to 13 (index rebuilds on next run). Built over the language-agnostic chunk/FTS tables; the comment extractor handles C-family and hash comments, so it carries to more than one language.
- Result (measured, `localize.s2.json`, 108 PRs, title only, no model, vs the S1 baseline): overall returned-set recall 18.3 to 18.8 percent, precision 10.7 to 11.7 percent, identifier-rich 30 to 32 percent, test-only 31 to 36 percent, false-rejection on answerable 7.1 to 5.1 percent, precision when confident 19.4 to 23.3 percent (back over the 20 percent target). Natural-language domain flat at 17 percent (the corpus PR titles share little stemmed vocabulary with the comments here, an honest corpus limit). Graded states confident 20, partial 74, insufficient 14.
- Verification: `dotnet build` 0 errors; `dotnet test Fuse.slnx` green (new `PorterStemmerTests` with canonical Porter outputs, `CommentExtractorTests`, and added `WorkspaceIndexFtsTests` for stemmed match and comment-only localization; Fuse.Indexing.Tests rose to 58). Golden `v3-localize` regenerated (stronger corroboration flattened the top into a partial 3-candidate set). Format clean.
- Blockers: none.
- Lessons: the stemmer must be conservative (letters-only, length gate) or it mangles code tokens and digits; keeping it a separate low-weight column rather than re-tokenizing the whole FTS table preserved exact-identifier ranking with no risk to the existing columns. The comment bridge moved comment prose from body weight (0.7) to its own field (1.0) plus the stems bridge, but in a mostly-syntax corpus where comments were already in the body the marginal gain is small and lands off the natural-language bucket; reported straight.
- Time: one session.

### S5 Structural priors: dependency centrality landed, co-change deferred (2026-06-27)

- Status: partial. The dependency-centrality prior landed and is verified; the git co-change prior is deferred (box left unticked) because its data is not collected.
- Result: a public `GraphCentralityPrior` (in `Fuse.Retrieval`) computes normalized node degree over the semantic graph (`GetAllEdgesAsync`) and, in the localize path after scoring, multiplies each leading candidate's score by a small capped factor (at most plus ten percent) proportional to its file's centrality, then re-ranks before grading. A widely-depended-on file outranks a leaf for an otherwise-tied query; the multiplier is on the existing score, so it tunes and cannot promote a near-zero-score file. It reads the language-agnostic node and edge tables and is a no-op in syntax mode (no edges).
- Result (measured, `localize.s5.json`, 108 PRs, title only, no model, vs the S2 baseline): corpus-wide localize is unchanged (precision when answered 13.5 to 13.7 percent, all buckets and refusal metrics identical), because the four syntax repositories have no edges and the two partial repositories barely move. An honest ceiling of the mostly-syntax corpus, not the prior; the prior's correctness is shown by the unit tests, not by a corpus number it cannot move here.
- Verification: `dotnet build` 0 errors; `dotnet test Fuse.slnx` green (new `GraphCentralityPriorTests`: central candidate outranks leaf at a tied score, the bounded multiplier cannot lift a weak central candidate over a strong one, and no edges is a no-op; Fuse.Retrieval.Tests rose to 63). Golden `v3-localize` regenerated (centrality elevated the central interface to a confident 2-candidate set on the fixture, which has edges). Format clean.
- Blockers: the git co-change prior is genuinely blocked on missing collected data. The `git_cochange` table is defined in the schema (path_a, path_b, count, pmi, jaccard) but no code in the repository writes to it, contrary to the plan's premise that it "is collected but not used." Populating it needs a git-history co-change miner: parse `git log --name-only` over a bounded window (with the bounded external-process-args invariant, chunked or via stdin), count file pairs that change together (capping or skipping wide commits to bound the pairwise blowup), compute the counts, and upsert. That is a sizable, performance-sensitive feature (it would add to every cold index), so it is deferred as its own unit rather than rushed; the centrality half delivers the structural-prior capability in semantic and partial mode in the meantime.
- Lessons: a "tie" at the engine level is hard to construct through the lexical channel because the rank-decay scorer separates equal-BM25 hits by rank, so the prior is best tested in isolation (the reason `GraphCentralityPrior` is public) rather than through a crafted query; the corpus cannot demonstrate the graph-dependent priors because it loads mostly syntax, which is the same ceiling that bounds S5 through S8 here.
- Time: one session.

### S7 Graph-aware discovery (2026-06-27)

- Status: landed as an opt-in operating point.
- Result: after the candidate set is selected, `ExpandSeedsThroughGraphAsync` expands the top one or two seeds one hop through the typed semantic graph (reusing the existing `GraphExpansionEngine`), converting a file-only lexical seed to its declared nodes so a text hit can still traverse, and admits a capped set of new neighbor files (a new `CandidateSource.GraphNeighbor`) at their decayed expansion score, then re-ranks. So a query that lexically hits only an interface also returns its implementation and a caller, even with no shared vocabulary. Bounded by seed count (2), depth (1), and neighbor cap (5); a no-op in syntax mode. Off by default and enabled per request (`ExpandGraph`, surfaced as MCP `expand` and CLI `--expand`).
- Result (measured, two operating points over the 108 PRs, no model): default off (`localize.s7.json`) is byte-identical to S5 (recall 18.9 percent, precision 11.9 percent, median 1,098 tokens), no regression; expansion on (`localize.s7-expand.json`) moves recall 18.9 to 19.1 percent at a precision cost (11.9 to 10.8 percent) and a higher median (1,352 tokens). A poor trade against changed-files-only ground truth because only two corpus repositories carry a graph and the blast radius reads as noise to that metric, so expansion is offered rather than defaulted; its discovery value is real where a graph exists, shown by the interface-to-implementation fixture.
- Verification: `dotnet build` 0 errors; `dotnet test Fuse.slnx` green (new `GraphDiscoveryTests`: expands interface to implementation with no shared vocabulary; degrades to lexical with no edges; Fuse.Retrieval.Tests rose to 65). Golden `v3-localize` unchanged (default off). Format clean.
- Blockers: none, but the recall headroom is bounded by the mostly-syntax corpus.
- Lessons: expanding before grading let the confident tight-set selection drop the very neighbors expansion added, so expansion must run after selection (enrich the answer) rather than before (dilute the grade); and because the corpus penalizes blast radius on a changed-files-only metric, making expansion opt-in and reporting both operating points is the honest framing the plan calls for rather than forcing a precision regression into the default.
- Time: one session.

### S8 Iterative exploration primitives (2026-06-27)

- Status: landed (primitives and tool); the agent-funnel benchmark is deferred to the larger Suite D budget.
- Result: a public `GraphNeighborhoodExplorer` (in `Fuse.Retrieval`) provides three deterministic, body-free navigation primitives, each ranked, bounded, and provenance-tagged: the graph neighborhood of a file (outgoing and incoming typed edges through its declared nodes, plus same-folder cohesion so a sparse syntax-mode result is never empty), the callers and implementers of a symbol (incoming edges of its node), and the structurally central files of an area (node degree over a folder prefix, or the whole workspace). Exposed as a new read-only MCP tool `fuse_neighbors` (specify one of `file`, `symbol`, or `centralIn`). Built over the language-agnostic node, edge, and file tables, so it carries to a future language.
- Result (verification of behavior, not a corpus metric): unit tests on a seeded graph show the neighborhood returns the implementer (outgoing di_resolves_to) and the caller (incoming di_injects) and never the seed itself; callers-and-implementers returns the incoming dependents with edge provenance; central-files ranks the highest-degree file first; with no edges the neighborhood falls back to same-folder cohesion. The MCP tool list test was updated from eight to nine V3 tools.
- Verification: `dotnet build` 0 errors; `dotnet test Fuse.slnx` green (new `GraphNeighborhoodExplorerTests`, Fuse.Retrieval.Tests rose to 69; the MCP stdio tool-list test updated to include `fuse_neighbors`). Format clean.
- Blockers: the agent-funnel measurement (Suite D: higher recall at fewer tool calls and tokens than native exploration) needs real Claude Code CLI rollouts, the larger compute budget already flagged for Suite D, so it is deferred and noted rather than run here. Co-change neighbors are omitted pending the S5 co-change collector.
- Lessons: the primitives compose the S5 centrality and S7 expansion machinery into agent-shaped operations, so most of the work was already in place; the honest unit of work here is the deterministic primitives plus their tests, with the model-dependent funnel measurement called out as the heavier follow-up.
- Time: one session.

### S10 Multi-language foundation: syntax-tier seam and spike (2026-06-27)

- Status: the load-bearing syntax-tier seam and the second-language spike landed; the language tag and the semantic-tier seam remain (box left unticked).
- Result: a `ILanguageSyntaxProvider` capability surface (in `Fuse.Semantics`, because `Fuse.Plugins.Abstractions` is dependency-free and the extraction record types live in `Fuse.Indexing`) claims file extensions and extracts symbols and chunks with no compiler. `SemanticIndexer` now selects providers by extension through a `LanguageSyntaxProviderRegistry` and drives the file scanner from the registered providers' extensions (plus the .NET config set), so a non-C# extension is surfaced without hardwiring. The existing Roslyn-syntax extractor became the `CSharpSyntaxProvider` behind the seam (no behavior change; routes stay a C# concern), and a minimal `PythonSyntaxProvider` (a bounded line lexer for `class` and `def`, public/private by leading underscore) is the spike. Built internally in the indexer constructor so no constructor, DI, or test signature changed.
- Result (measured): no regression after the refactor. Suite A is unchanged (22 of 22 edges, recall and precision 1.0); corpus localize is unchanged (recall 18.9 percent, `localize.s10.json`). A mixed-language workspace test indexes a `.cs` and a `.py` file at the syntax tier and confirms both are found, and the Python file is full-text-searchable by an identifier subword (the S1 feature carrying to the spike language over the language-agnostic tables, for free).
- Verification: `dotnet build` 0 errors; `dotnet test Fuse.slnx` green (new `MultiLanguageSeamTests`: the Python provider extracts classes and functions; the registry selects by extension and reports its extensions; a mixed-language workspace indexes and finds the non-C# file; Fuse.Semantics.Tests rose to 83). Format clean.
- Blockers: none for the landed scope. Remaining (logged below): a `language` tag column on files, symbols, and nodes (a schema bump plus a record-field ripple across construction sites); a semantic-tier provider seam (the typed-graph builder stays C#/Roslyn); and namespacing the wiring analyzers by language. The semantic-tier work is the larger half and is deferred to a focused session.
- Lessons: the seam interface belongs where the extraction types are visible (`Fuse.Semantics`), not in the dependency-free abstractions assembly as the plan's wording suggested; constructing the provider registry inside the indexer avoided a constructor/DI/test ripple while still making the syntax tier load-bearing; and the no-regression guard (Suite A plus the C# golden and unit tests) was cheap to run and is the right gate for a refactor that must not move the C# numbers.
- Time: one session.

### Remaining after this session (S5 co-change, S6, S10 tag and semantic seam)

Landed and pushed on `feature/v3.1` (PR #21) this session: S3, S1, S2, S5 (centrality half), S7, S8, S10 (syntax seam and spike). Remaining, each with concrete scope, left unticked and surfaced rather than rushed:

- S5 co-change prior: blocked on missing data. The `git_cochange` table has no collector; needs a git-history co-change miner (parse `git log --name-only` over a bounded window with the bounded external-process-args invariant, count file pairs skipping wide commits, upsert), plus the retrieval blend. A performance-sensitive feature that adds to every cold index.
- S6 dependency-scoped incremental re-index (freshness): `ReindexFileAsync` today refreshes only a changed file's own syntax rows (its docstring states this explicitly). Refreshing cross-file semantic edges incrementally needs the analyzer pipeline (`SemanticAnalysisRunner`) re-run over the changed file's compilation neighborhood (its project and direct dependents), which only exists in semantic mode. The correctness check (post-incremental edges match a full re-index on the changed neighborhood) is demonstrable on the in-repo wiring fixture, not the mostly-syntax corpus; the freshness timing is measurable on the `performance` suite. A sizable indexer change.
- S10 remaining (the syntax-tier seam and the Python spike landed this session): a `language` tag column on files, symbols, and nodes with a schema bump (a record-field ripple across construction sites, so it is a contained but broad change); a semantic-tier provider seam so a future language can supply a typed-graph builder (the larger half: the MSBuild/Roslyn loaders move behind a C# semantic provider); and namespacing the wiring analyzers by language so a future provider registers its own detectors. The semantic-tier seam is deferred to its own focused session so the C# semantic path is not destabilized mid-refactor.

The V3.1 retrieval features that did land (S1, S2, S5-centrality, S7, S8) were all built over the language-agnostic symbol, chunk, and edge tables, not the C# path, so they already carry to a future language once the S10 seam exists.

### A3 Corpus rebuild for semantic mode (2026-06-27)

- Status: landed. The benchmark corpus is rebuilt to repositories that restore and index in semantic or partial mode on the .NET 10 SDK.
- Result: replaced the prior syntax-only set (MediatR, FluentValidation, AutoMapper, Newtonsoft.Json, Serilog) with three libraries that restore clean on .NET 10 (Scrutor 7f315dab, Ardalis.Specification d2fcf351, NodaTime 59692eaa), keeping the eShopOnWeb application (MIT) and the in-repo SampleShop fixture. `corpus.json` repinned with commits, dates, and cs-file counts plus per-repo notes. `prs.json` regenerated from merge history via `gen-prs.ps1`: 53 PRs (Scrutor 3, Specification 14, NodaTime 18, eShopOnWeb 18), dropping 3 maintenance-title mismatches. One adjudicated `reading_set` curated on eShopOnWeb#300 (the `IBasketService` interface its changed service implements) so the adjudicated-recall path and its test stay exercised.
- Result (measured): Suite A unchanged (fixture-based: 22 of 22 edges, recall and precision 1.0). Suite B review (`review.json`, 53 PRs, `--restore`): changed-file recall 100 percent, precision 79.8 percent, F1 0.89, median 958 / mean 1,095 tokens, index modes partial 27, semantic 1, syntax 25 (was partial 12 of 48 on the prior corpus, so the moat is now visible), grep baseline 53 percent recall at 14 percent precision. Suite C localize (`localize.json`, 53 PRs, title only, no model, `--restore`): overall recall 13.1 percent at 8.8 percent precision, median 835 tokens; low-signal detection F1 1.0, false-rejection on answerable 5.8 percent, precision when confident 18.5 percent; buckets identifier-rich 20 percent (n 15), nl-domain 14 percent (n 29); localize main-checkout index modes partial 2, syntax 2. This is the honest open-ended baseline A1 (dense) and the graph-dependent priors are measured against.
- Verification: `dotnet build Fuse.slnx -c Release` 0 errors; `dotnet test Fuse.slnx -c Release --no-build` green (Fuse.Benchmarks.Tests updated: manifest asserts Scrutor present, dataset 53 tasks across 4 repos, reading-set adjudicated PR present; all 39 pass, full suite green); `dotnet format Fuse.slnx --verify-no-changes` clean; site MDX compiles (`fumadocs-mdx`). AGENTS.md measured-results section and `site/content/docs/project/benchmarks.mdx` resynced (new corpus table, Suite B/C rebuilt-corpus numbers, prior-corpus suites labelled).
- Blockers: none that block the rebuild. Two honest caveats recorded rather than hidden: (1) Polly was rejected because its pinned `global.json` requires SDK 10.0.301 (not installed) and a Cake MSBuild SDK; (2) Scrutor uses central package management with two NuGet sources and, nested under Fuse's repository NuGet config, hits a deterministic NU1507 source-mapping conflict and restores to syntax in the in-tree localize checkout (it restores clean in isolation and contributes its DI/decoration source regardless). The agent (Suite D), reduction (Suite E), R4 adjudicated review, peer, and performance suites are not re-run here and retain their prior-corpus recordings, labelled as such; they re-run on the rebuilt corpus in A1, A2, and A4.
- Lessons: review reaches partial/semantic broadly because it restores each PR's worktree in a temp directory outside the repository tree, dodging the nested-NuGet-config conflict that caps the in-tree localize main-checkout index mode; the right honest framing is to report both index-mode distributions rather than force one number. Squash-merge repositories (Scrutor, 4 merge-PR commits) yield few `prs.json` PRs, so a repo's value splits into resolution/source coverage versus PR-ground-truth yield; the rebuilt corpus balances both.
- Time: one session.

### A2 Peer benchmark harness at scale (2026-06-27)

- Status: landed. The layer-6 peer harness now runs all four arms head-to-head on the rebuilt corpus, so a peer claim is falsifiable rather than asserted.
- Result: added a `serena` arm to `layer6-peers.ps1` (oraios/serena, LSP-backed symbol search, driven via one headless `claude` rollout restricted to `mcp__serena`, env-gated on `SERENA_CMD`), alongside the existing `coa` arm (anortham/coa-codesearch-mcp, .NET Lucene MCP host, built from source, gated on `COA_CODESEARCH_EXE`). Added `-ModelPerRepo` so the costly model-driven arms run a bounded per-repo sample while the deterministic arms (fuse, codegraph) scale wide, with both per-arm sample sizes recorded so a bounded model run is never mistaken for the full one. Extracted `Score-Set` into `common.ps1` as one shared, tested implementation and removed the inline copy.
- Result (measured, `layer6-peers.json`, rebuilt corpus, 50,000-token budget, 12 deterministic PRs / 4 model-driven PRs): fuse recall 19 percent at 19 percent precision (12 PRs), codegraph 9 percent at 11 percent (12 PRs), coa 9 percent at 1 percent (4 PRs), serena 34 percent at 27 percent (4 PRs). Honest reading: Fuse beats CodeGraph decisively on the 12-PR deterministic comparison (19 versus 9 percent recall at higher precision); Serena's higher aggregate is a 4-PR sample dominated by one tiny-repo outlier (Scrutor#6, a 2-file change in a 45-file repo, where its symbol search returns the whole repo for 100 percent recall), while on the two substantive model-driven PRs Fuse leads or ties it (eShopOnWeb#949 38 versus 12 percent, NodaTime#621 33 versus 22 percent). coa returns long ranked path lists at near-zero precision. No head-to-head dominance claim is published beyond this measured, caveated reading.
- Verification: all four installs verified (codegraph present, claude present, coa built clean from source on .NET 9, serena resolves and runs via uvx); the 4-arm run produced 32 rows with each arm recording a result and none stubbed. `dotnet build` 0 errors; `dotnet format` clean; full `dotnet test` green except the known Fuse.Fusion SQLite parallel-load flake (passes 237/237 in isolation, unrelated to A2 which changed no C#). Harness Pester tests rose from 11 to 17 (new `Score-Set` determinism block: perfect set, partial, order-independence, dedup, backslash-normalize, empty set). AGENTS.md peer bullet and `benchmarks.mdx` peer section resynced with the four-arm table and the honest sampling caveat; the A3/A2 banner and the not-measured list updated.
- Blockers: none on availability. The model-driven arm scale is compute-bounded: each coa/serena rollout is one `claude` call (MCP server start, per-project index, one search), expensive and not byte-reproducible, so a full 50-to-100-PR model-driven run is left as a larger compute budget (the same ceiling Suite D carries), with the deterministic arms scaled wide instead. coa was pinned to an archived repo (`anortham/coa-codesearch-mcp`) that still builds on .NET 9.
- Lessons: the fair comparison is per-arm-sample-aware, not the raw aggregate, because the model-driven arms ran a smaller, different PR set; reporting both sample sizes and reading the substantive PRs separately from a tiny-repo outlier is what keeps the claim honest. Serena's per-project C# LSP index is the slowest arm but completes inside the rollout backstop on this corpus. The peer scoring (`Score-Set`) belongs in `common.ps1` as one tested function so the number that backs the claim has a determinism test.
- Time: one session.

### A1 Dense by default (2026-06-27)

- Status: landed. The dense embedding channel is on by default and the no-model floor is retired.
- Result: `FUSE_DENSE` is now an opt-out (dense enabled unless it is explicitly falsy: `0`, `false`, `no`, `off`), implemented as a single source of truth `DenseModelProvisioner.IsDenseEnabled`. `AddOnnxTextEmbedder` registers the `ITextEmbedder` whenever dense is enabled (the model-presence gate at registration is dropped; the embedder loads lazily and reports unavailable until the model is cached, so registration is safe before provisioning). A new `DenseModelProvisioner.EnsureModelAsync` fetches-and-caches the bundled bi-encoder model once (about 23 MB, the integrity-pinned `RerankModelDownloader`), idempotent and offline-safe (a present model or a disabled channel is a fast no-op; a failed fetch logs and returns, leaving the lexical fallback). It is wired into the index entry points only (`IndexCommand`, MCP `FuseIndexAsync`, and the lazy `OpenIndexedAsync`), never the query path, so query time stays offline. The no-rewrite rule holds: the raw query string is embedded, never paraphrased. The eval command flips to dense-by-default with a `--lexical` opt-out for the A/B (the old `--dense` flag is replaced). No `Fuse.Cli.Rpc` DTO or `[JsonRpcMethod]` change (the index and localize tools are MCP string tools), so the protocol version is untouched; no index schema change (existing indexes lacking vectors degrade to lexical until a full re-index, which is graceful, not a regression).
- Result (measured, `localize.json` dense-default versus `localize.a1-lexical.json` lexical fallback, 53 PRs on the rebuilt corpus, title only, restore on): overall returned-set recall 13.3 to 15.1 percent, natural-language-domain bucket 14 to 17 percent (the bucket dense targets), identifier-rich 20 to 21 percent, false-rejection on answerable queries 5.8 to 0.0 percent (insufficient drops 4 to 1, partial rises 40 to 43), at a precision-when-confident cost 18.5 to 9.3 percent (dense pulls a few semantically-near-but-wrong files into the answered set on this small, mostly-syntax corpus) and a median 835 to 1,087 returned tokens; overall returned-set precision flat within noise (9.0 to 8.7 percent). Low-signal detection F1 holds at 1.0. The recall and false-rejection win at a precision-when-confident cost is the recall-versus-precision pressure the plan flags; the graded cutoff (S3) and the learned ranker (S9) are the counterweights. Model size 22,972,370 bytes.
- Verification: `dotnet build Fuse.slnx -c Release` 0 errors; `dotnet test Fuse.slnx -c Release --no-build` green except the known `FusionConcurrencyTests` SQLite parallel-load flake (passes 237/237 in isolation, unrelated to A1 which touched no Fusion code); `dotnet format Fuse.slnx --verify-no-changes` clean. New tests: `DenseModelProvisionerTests` (default-on, the opt-out matrix, and the disabled no-op fetches nothing; Onnx.Tests rose to 22) and a `DenseCandidateGenerator` no-rewrite test asserting the raw query is embedded verbatim (Retrieval dense tests rose to 4). Docs resynced: `reference/configuration-keys.mdx` (FUSE_DENSE now on-by-default opt-out), `concepts/scoping.mdx` and `internals/scoping-internals.mdx` (dense on by default, fetched once and offline, no-rewrite, graceful lexical fallback), `project/benchmarks.mdx` (the A1 dense-versus-lexical A/B table), and the AGENTS.md measured-results localize bullet.
- Blockers: none. One honest caveat: the dev-machine model cache was already present, and the early A/B run silently used a stale host binary (the full-solution build did not refresh the apphost output, so dense never engaged and the numbers matched the lexical baseline); rebuilding the host project produced the real A/B. The fetch-once path is exercised on a fresh cache in real use; the offline-graceful-fallback path is covered structurally and by the disabled no-op test rather than by a network-failure unit test (the download URLs are fixed, so a true offline failure is not unit-testable here).
- Lessons: register the embedder unconditionally when enabled rather than gating on model presence, because gating at DI time freezes the decision before provisioning can run; keep provisioning off the query path so the offline-at-query-time rule is structural, not incidental; and after a full-solution build always confirm the host apphost actually refreshed before trusting a benchmark, since a stale `fuse.exe` reads as a no-op result that looks like a real (null) measurement.
- Time: one session.

### A4 Cold-start fix, partial; S6 deferred (2026-06-27)

- Status: partial. Syntax-first serve, background semantic upgrade, parallel per-file extraction, and a cold-start measurement landed; the resident-Roslyn-workspace reuse and S6 (dependency-scoped incremental semantic edges) are deferred together with concrete scope (box A4 marked partial, S6 left unticked).
- Result: a public `SemanticIndexer.IndexSyntaxFirstAsync` indexes the syntax tier only (no MSBuild, no embedding), sets index mode `syntax` and a readable `semantic_pending` meta flag (`SemanticIndexer.SemanticPendingMetaKey`); a public `UpgradeToSemanticAsync` runs the full pass and clears the flag. The full `IndexAsync` also clears the flag, so any complete pass is the final word on the mode. Per-file syntax extraction is parallelized (`Parallel.ForEachAsync`, bounded to the processor count) and collected positionally, so the flattened symbol/chunk/route stream is byte-identical to the sequential pass (a determinism test guards this). The MCP host `OpenIndexedAsync` serves syntax-first on a cold read and schedules the semantic upgrade as a guarded fire-and-forget on its own background store (a `ConcurrentDictionary` guard prevents duplicate upgrades per root; failures are swallowed so the server never crashes and the syntax tier stays usable). The background upgrade is opt-in via `FUSE_BG_UPGRADE=1` (set on `FuseTools.BackgroundSemanticUpgradeEnabled` by `McpServeCommand`); by default the first read indexes synchronously (the proven-stable path), so no detached background task outlives a request. Making the syntax-first cold start the default is deferred with the resident-Roslyn-workspace work that lets the long-lived host manage the background task's lifetime cleanly (a detached fire-and-forget task in the serve subprocess proved too easy to orphan or race under test teardown). The integration test's fixture teardown also retries its delete to tolerate a transient file-handle race. The CLI `fuse index` and MCP `fuse_index` stay synchronous full passes. The syntax-first pass deliberately skips embedding (A1 dense) so it serves fast; the background upgrade embeds, so dense lands with the graph.
- Result (measured, `performance.json`, rebuilt corpus, `fuse eval performance --repo NodaTime --restore`): syntax tier served in about 20 seconds (mode syntax, `semantic_pending=1`) versus about 70 seconds for the full semantic pass, a 3.5x reduction in time to first answer; the upgrade clears the flag. The residual 20 seconds is the file scan, the parallel parse, and the full-text index construction (subword, stem, comment fields) plus first-call JIT, not MSBuild, so the few-seconds ideal is not met on a 512-file repo; closing it is the deferred resident-workspace work. Warm operations unchanged (warm localize 61 ms P50, review plan 110 ms P50, incremental re-index 23 ms P50). NodaTime loads syntax mode here (the corpus-pinning ceiling), so the upgrade is another syntax pass; the split still demonstrates the MSBuild load removed from the first answer.
- Verification: `dotnet build Fuse.slnx -c Release` 0 errors; targeted `dotnet test` green (new `SemanticIndexerTests`: syntax-first serves and flags pending, upgrade clears the flag and lands the graph, parallel extraction is deterministic across runs; Fuse.Semantics.Tests rose by 3); full suite and format run before commit (the known `FusionConcurrencyTests` flake aside). Docs resynced: `project/performance.mdx` (the cold-start syntax-first split and numbers), `internals/pipeline.mdx` (syntax-first then background semantic), and the AGENTS.md cold-start bullet.
- Blockers: the resident-Roslyn-workspace reuse and S6 incremental edges are genuinely coupled and warm-host-concurrency-sensitive. A correct S6 (edited-file semantic edges current in under 1 second) needs the changed file's compilation neighbourhood resident so the wiring analyzers re-run without a fresh MSBuild load; without the resident workspace, recomputing edges means a project reload (seconds, not sub-second), which misses the target. Both are deferred to a focused session rather than rushed alongside the rest of Wave 2, exactly as Wave 1 deferred S6 and the S10 semantic-tier seam.
- Lessons: the cold-start cost is MSBuild-dominated, so syntax-first serve (not parallelism) is the lever, and embedding (A1) must be deferred off the fast path or it re-adds about 40 seconds; a fire-and-forget background upgrade in a long-running MCP server must own its store lifetime (the foreground store is disposed when the tool returns) and swallow failures so a background fault never takes down the server.
- Time: one session.

### A6 Git co-change collector and prior (2026-06-27)

- Status: landed (the deferred S5 co-change half). The collector and the retrieval prior both ship; on by default.
- Result: a self-contained `GitCoChangeCollector` (in `Fuse.Semantics`, its own bounded git invocation since `GitExecutableLocator` lives in `Fuse.Fusion` which `Fuse.Semantics` does not reference) mines up to 1000 recent non-merge commits with one `git log --name-only` pass (a fixed argument list, so the external-process command line is bounded by construction, honoring the bounded-args invariant), counts source-file pairs that change together (wide commits over 40 source files skipped, non-source files ignored, pairs seen at least twice kept), computes count, PMI (log2), and Jaccard, and writes the `git_cochange` table (which already existed in the schema with no collector, so no schema bump). `SemanticIndexer.IndexAsync` calls it best-effort at the end of a full pass (not the syntax-first fast path); a non-repository or git failure is a no-op. New store API `UpsertCoChangesAsync` (clears and rewrites, so a re-mine is authoritative) and `GetCoChangesForAsync` (a bounded parameterized IN-list over the seed set, matching either column). A new `GitCoChangePrior` (in `Fuse.Retrieval`), applied after the centrality prior, boosts a candidate that co-changes with a strictly stronger candidate by a capped factor (at most plus fifteen percent of the Jaccard), bounded so it cannot promote a near-zero-score file; a no-op with no mined data. Built over the language-agnostic file paths.
- Result (measured, `localize.json` dense default plus the prior versus `localize.a1.json` dense-only, 53 PRs, title only, restore on): overall recall 15.1 to 14.9 percent, precision 8.7 to 8.1 percent, nl-domain and identifier-rich buckets unchanged (17, 21 percent), median 1,087 to 1,033 tokens; within the recall CI, a hair down. The same corpus ceiling as S5 centrality: a changed-files-only ground truth over a mostly-syntax corpus scores a recovered co-change sibling as noise, not a hit, so the corpus cannot demonstrate the prior even where the mechanism is correct. Kept on by default (like S5) because the mechanism is unit-verified and helps where multi-file changes share git history.
- Verification: `dotnet build Fuse.slnx -c Release` 0 errors; new tests green: `GitCoChangeCollectorTests` (aggregation, PMI/Jaccard, wide-commit skip, non-source-file ignore, one-off drop, over synthetic git-log text so no git executable is needed; Fuse.Semantics.Tests +3), a `WorkspaceIndexStoreUpsertTests` co-change round-trip (either-column match, re-mine replaces; +1), and `GitCoChangePriorTests` (sibling boosted above a non-co-changer, bounded cannot promote near-zero, no-op with no data; Fuse.Retrieval.Tests +3); full suite and format run before commit. Docs resynced: `internals/scoping-internals.mdx` (the co-change prior), `internals/caching-internals.mdx` (the miner and the table), `reference/configuration-keys.mdx` (the fixed caps), `project/benchmarks.mdx` (the A6 paragraph), and the AGENTS.md S5-deferral note plus a new A6 measured-results bullet.
- Blockers: none. The corpus cannot move the metric for the same reason S5 could not; the honest framing is unit-test correctness plus a within-noise corpus number, not a fabricated gain.
- Lessons: the prior must boost only by a strictly stronger co-changer (not by seed membership), or with few candidates every candidate is a seed and nothing is boosted; the determinism of the collector is best tested by feeding synthetic git-log text to a public `ParseLog` rather than standing up a real git repo, which also keeps the test git-independent.
- Time: one session.

### P1 Messaging source of truth (2026-06-27)

- Status: landed. An internal messaging reference the landing page, README, and launch material draw from.
- Result: new `site/content/docs/project/messaging.mdx` (added to the project meta) states the one-sentence pitch (a faster, cheaper, more accurate AI assistant on a .NET codebase), the plain benefit framing (accuracy, cost, speed), the message hierarchy (outcome leads; raw token-reduction percentages and internal architecture are demoted), the surface-versus-depth layering rule (top-of-funnel stays abstract; concept/reference/internals carry the mechanism), the differentiation framing as capability differences not measured wins (versus grep, whole-repo packers, embedding-only RAG, tree-sitter graph tools, and code-search MCP servers), the .NET-first wedge, the honesty pillar (every number sourced, weaknesses published, no head-to-head win before A2/A3 back it, refuse-and-route as a trust feature), and the exact sourced numbers other surfaces may quote (review median 958 tokens at 100 percent recall, Suite A 22 of 22 edges, warm latency, reduction support number, and the weak open-ended ~15 percent stated too).
- Verification: `fumadocs-mdx` compiles; the file is plain ASCII (verified); eslint does not lint MDX prose (no matching config), so the compile is the content gate; every quoted number is sourced to `tests/benchmarks/results`. No code changed, so the dotnet gates are unaffected.
- Blockers: none.
- Lessons: writing the messaging contract first gives P2 (landing) and P3 (README) a single source so they cannot drift or overclaim; the honesty rules (sourced numbers, no unmeasured head-to-head) are encoded here so every later surface inherits them.
- Time: part of one session.

### P3 README rewrite (2026-06-27)

- Status: landed. The README is rewritten agent-first against the P1 messaging contract and resynced to current numbers.
- Result: the hero is the outcome (a faster, cheaper, more accurate AI assistant on .NET) with the wiring sentence as the one concrete hook; install one-liner plus the copy-paste MCP config block (same `command`/`args` for Claude Code, Cursor, generic MCP client); a quickstart showing the two highest-value verbs (`fuse_resolve` and `fuse_review`) with sample output; the tool surface as the verbs an agent uses; an honest benchmarks section; and a status section (solid .NET semantic, default-on offline dense, early multi-language). The stale inline benchmark figure (old-corpus numbers baked in) was removed in favor of a sourced text section linking the benchmarks page, since the image cannot be trusted until regenerated (P4).
- Result (numbers resynced to `tests/benchmarks/results`): replaced the superseded prior-corpus figures (5-library corpus, 10 of 10 edges, 48 PRs at 89.6 percent / 874 tokens, 27 percent localize, 6-PR agent 135K/212K) with the current ones: corpus Scrutor/Specification/NodaTime/eShopOnWeb, Suite A 22 of 22 edges, review 53 PRs at 100 percent recall / 79.8 percent precision / median 958 tokens with the grep baseline, localize about 15 percent (the weak mode, with the refuse-and-route note), agent R8 12 PRs (30 vs 26 percent), reduction 37 to 55 percent. No unmeasured head-to-head claim; the peer comparison is linked, not restated.
- Verification: README is plain ASCII (verified); the MCP config snippet is copy-paste correct; every quoted number is sourced. No code changed.
- Blockers: none.
- Lessons: the README had drifted to the V2 CLI-first framing and prior-corpus numbers; rewriting against the P1 contract (and removing the stale figure) is what keeps the public surface honest, and it is the natural moment to resync every quoted number to the rebuilt-corpus results.
- Time: part of one session.

### P5 Honest comparison table (2026-06-27)

- Status: landed (unblocked: A2 ran the four-arm harness and A3 rebuilt the corpus). The peer comparison on the benchmarks page now carries the exact reproduction command.
- Result: the Peer comparison section (added by A2) already publishes the four-arm table (fuse, codegraph, coa-codesearch, serena) with per-arm sample sizes and the honest reading (the deterministic 12-PR comparison where Fuse leads CodeGraph, and the bounded 4-PR model-driven sample where Serena's aggregate is a tiny-repo outlier while Fuse leads or ties on the substantive PRs, with token columns flagged as not directly comparable). P5 adds the exact reproduction command block (the `layer6-peers.ps1` invocation, deterministic-only and the env-gated model-driven form), states where Fuse does not lead, and records that omitted peers are marked omitted, never stubbed, with the full 50-to-100-PR model-driven run named as the remaining work. No ranking is claimed beyond what the harness backs.
- Verification: `fumadocs-mdx` compiles; the section is plain ASCII (verified); every cell is sourced to `tests/benchmarks/results/layer6-peers.json`; the reproduction command matches the harness usage. No code changed.
- Blockers: a full model-driven peer run at scale remains compute-bounded (the same ceiling A2 recorded), so the published table keeps its small-sample caveat rather than overstating.
- Lessons: P5 was mostly satisfied by A2's measured table; the gating discipline (no comparison before the harness backs it) meant the only new work was making it reproducible (the command) and explicit about where Fuse loses, which is the honesty pillar applied to the comparison.
- Time: part of one session.

### P2 Landing page rewrite (2026-06-27)

- Status: landed. The landing page (`site/src/app/(home)/page.tsx`) is rewritten simple and outcome-first against the P1 contract.
- Result: the hero leads with the benefit (a faster, cheaper, more accurate AI assistant on your code) and carries the one "see it" artifact (a without-Fuse-versus-with-Fuse before/after of the same wiring question, grep-and-open versus `fuse_resolve` plus `fuse_review` in one call). Three plain proof tiles replace the old metric strip (accurate wiring answers: 22 of 22 edges; a PR's context in about 958 tokens at 100 percent changed-file recall; warm answers in milliseconds), each sourced and linking the benchmarks page. The value props, the connect-your-agent block (copy-paste MCP config), and the capability comparison table were updated to current framing (offline dense by default, refuse-and-route, the agent verbs), and the stale benchmark figure (old-corpus numbers baked into the image) was removed in favor of the sourced tiles and the benchmarks link. The deep mechanism stays one click away (links to concepts and why-fuse), per the surface-versus-depth rule.
- Result (numbers resynced): removed the superseded hero claims (88 percent recall, ~13x tokens, 51 percent query recall, eight tools, the "explore phase collapsed" V2 framing) and the old-corpus figure; every shown number is now sourced to the rebuilt-corpus results.
- Verification: `npm run types:check` passes (fumadocs-mdx, next typegen, tsc --noEmit, exit 0); `eslint` on the page is clean; the file is plain ASCII (verified). The now-unused `HeroVisual` component file is left in place (harmless; not imported).
- Blockers: none.
- Lessons: the landing page had drifted to V2 packer-era numbers and a stale figure; rewriting it against the P1 contract with a concrete before/after artifact (not a decorative visual) is what makes the outcome legible, and removing the baked-in figure is the honest move until P4 regenerates one.
- Time: part of one session.
