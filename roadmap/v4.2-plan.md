# Fuse V4.2 Plan: close the architecture-review tails

V4.1 shipped the index/cache split, FTS availability surfacing, host served-root binding,
workspace path confinement, opt-in metrics, and the first wave of freshness and install
hardening tests (see `CHANGELOG.md` [4.1.0] and the 2026-07-13 architecture review in the
agent transcript). V4.2 closes every **open** review tail (F-005 through F-027) and the
dogfood MCP items (R13 through R21) in **one complete delivery per checklist row**: engine,
tests, and docs land together. No deferral, no partial implementation, no "follow-up" tails
left inside an item.

The honesty conventions are unchanged: every number sourced to `tests/benchmarks/results`,
weaknesses published, plain ASCII prose, the three gates green per item.

## Product philosophy (4.2)

Fuse is primarily an **agent surface**, not a tool humans drive directly. Humans install it
once (`fuse mcp install`); agents call MCP tools for the rest. The product bar is **it just
works**:

- No env-var archaeology for a normal Cursor or Claude Code session.
- Upgrade, schema drift, and corrupt derived data self-heal without asking the user to delete
  `.fuse` or read a stack trace.
- Operational states (`index_busy`, `upgrade_pending`) are returned to the agent as structured
  text the model can retry on, not opaque MCP failures.
- Breaking Fuse or index contract changes rebuild derived state automatically; source is never
  touched.

CLI and operator docs remain for maintainers and CI; defaults and `fuse mcp install` target the
agent path first.

## Where V4.2 starts (4.1 baseline)

Shipped in 4.1.0 (recorded in the review progress log):

- **F-001** cache/index split (`.fuse/fuse-cache.db` vs `.fuse/fuse.db`; index recovery refused)
- **F-002** FTS unavailable surfaced; `fuse_find kind=task` refuses honestly
- **F-003/F-004** storm reconcile and MCP read-after-edit tests
- **F-006** orphaned `ExperimentalOptions` query knobs removed
- **F-009/F-010/F-013** `SyntaxIndexer` deleted, dead csproj refs removed, `RankedFile` deleted
- **F-014/F-019/F-021/F-024/F-025/F-028/F-029** docs, config warnings, path resolver, install tests,
  resident storm eviction test, comment fixes
- **F-017** opt-in `FUSE_METRICS=1` (not OpenTelemetry; metrics behind env flag)
- **F-018** served-root binding on host RPC (partial; PipeSecurity still open)

Still open after 4.1 (this plan closes all of these fully):

| Bucket | Count | IDs |
|--------|-------|-----|
| Was deferred | 5 | F-007, F-011, F-020, F-022, F-027 |
| Was partial | 7 | F-005, F-008, F-012, F-015, F-018, F-023, F-026 |

## Execution protocol

Every checklist item (R1 through R21) follows the same contract. An item is `[x]` only when
**all** of the below are true. `[>]` in progress; `[!]` blocked (progress log names blocker);
`[-]` descoped only via an explicit written decision in this file's progress log, never by
shipping half an item.

### Per-item deliverables (all required)

1. **Engine** - the full behavior described in the item body; no "phase 1" or "MVP" left for a
   later row unless that later row is explicitly a separate checklist item.
2. **Tests** - automated tests that fail when the behavior is removed or regresses. Confirm the
   test command discovers and runs them (the count goes up or an existing test is strengthened).
   No item ships with "tests: none" except pure doc-deletion items (R2), which still carry a
   scripted verification step.
3. **Docs** - every user-visible or agent-visible behavior is documented on `fuse.codes` (MDX
   under `site/content/docs`) and named in `CHANGELOG.md` when behavior or defaults change.
   Maintainer-only items (R2 `briefing.md`, R8 profile artifact) still update the cited doc
   paths. `AGENTS.md` design invariants when the item touches architecture.
4. **Three gates** - `dotnet build Fuse.slnx -c Release`, `dotnet test Fuse.slnx -c Release
   --no-build`, `dotnet format Fuse.slnx --verify-no-changes`, plus the item's Validation
   commands and Gate criterion.
5. **Progress log** - append an entry (format in [v4-plan.md](v4-plan.md) Execution protocol):
   Preconditions, Shipped, Commands (pasted output), Tests (file paths and what each proves),
   Docs (paths touched), Deviations (must be empty for a PASS), Gate (criterion -> PASS).

### Hard bans

- **No partial implementation.** If scope must split, add a new checklist row in the same
  session; do not mark the original row done with a tail.
- **No deferral inside an item.** Optional tests, optional docs, and "future work" paragraphs
  in item bodies are forbidden. Either the item includes them or the item is descoped in writing.
- **No silent behavior changes.** Defaults, throw behavior, tool output shape, and on-disk
  formats are named in `CHANGELOG.md` with migration (rebuild, reconnect, reconfigure).
- **No untested code paths** on the shipped default. Opt-in flags may stay off by default but
  their code path still has tests when the item introduces or changes them.

## Execution checklist

Work top to bottom; skip only when a dependency is not met and record why in the progress log.

- [x] R1 CI split for `RequiresSdk` tests (F-023)
- [x] R2 Finish `briefing.md` drift cleanup (F-015)
- [x] R3 Delete `FtsCandidateGenerator` (F-008)
- [x] R4 Hook RPC verbose mode (F-020)
- [x] R5 Narrow `fuse update` process termination (F-022)
- [x] R6 Unified host/MCP/CLI contract doc (F-012)
- [x] R7 Host IPC hardening (F-018)
- [x] R8 Profile index hot path on a large repo (F-027)
- [x] R9 Split `WorkspaceIndexStore` into narrow stores (F-007)
- [x] R10 Unify scoping stacks (F-005)
- [x] R11 Semantic-tier provider seam, C# behind interface (F-011)
- [x] R12 Host RPC outcome assertions (F-026)
- [x] R13 Promote shared daemon to default-on (G5 completion)
- [x] R14 Index access coordinator and SQLite concurrency
- [x] R15 MCP and CLI operational error taxonomy
- [x] R16 Fast `fuse_workspace` status without blocking index
- [x] R17 Read-only store open for warm reads
- [x] R18 Decouple compiler tools from mandatory index open
- [x] R19 Daemon-owned index writes (G5 phase 2)
- [x] R20 Index-build progress in availability headers
- [x] R21 Agent-first zero-config defaults and self-healing store

---

## R1. CI split for `RequiresSdk` tests

The review added `[Trait("Category", "RequiresSdk")]` and `SkipException.ForSkip`, but CI
still runs the full suite on every agent. SDK-heavy tests can pass vacuously or flake when the
runner lacks a working toolchain.

- Work: add a `ci-sdk.yml` job (or a filtered leg on `ci.yml`) that runs
  `dotnet test Fuse.slnx -c Release --filter "Category=RequiresSdk"` on win-x64 and linux-x64
  with the .NET 10 SDK and bundled build-capture worker; keep the default PR job on
  `Category!=RequiresSdk` for fast feedback.
- Tests: CI workflow files are the test; add a unit test or script in `build/` that fails if
  `RequiresSdk`-tagged tests exist but neither workflow leg runs them. A PR that removes the
  trait must fail the SDK job.
- Docs: `site/content/docs/project/contributing.mdx` (two CI legs, what each proves, how to
  run locally); `CHANGELOG.md` if contributor workflow changes.
- Gate: both CI legs green on a PR that touches a `RequiresSdk` test; default PR leg excludes
  them; contributing page lists both commands.

## R2. Finish `briefing.md` drift cleanup

The opening was refreshed for nine MCP tools in 4.1; the body still references fourteen tools,
`ext/vscode/`, and protocol mirrors removed in D15.

- Work: sweep `briefing.md` for stale tool counts, the VS Code extension tree, shim/protocol
  coupling to TypeScript, and superseded benchmark figures. Align every number with
  `tests/benchmarks/results` and `AGENTS.md`. Either delete obsolete sections or mark them
  archival with a one-line pointer to `roadmap/v4-plan.md` history.
- Tests: add `build/verify-briefing.ps1` (or extend `verify-version.ps1`) that fails on forbidden
  patterns (`fourteen` tools, `ext/vscode` as product surface, stale MCP tool names). Wire into CI.
- Docs: `briefing.md`; one-line note in `roadmap/README.md` that briefing tracks shipped
  product, not the executable checklist.
- Gate: verification script green; `rg` audit pasted in progress log; no stale product claims.

## R3. Delete `FtsCandidateGenerator`

4.1 gated the flat per-source FTS generator behind `FUSE_FLAT_FTS=1`. Shipping default remains
`LexicalCandidateGenerator` only.

- Work: remove `FtsCandidateGenerator`, `FUSE_FLAT_FTS`, and all references. Keep
  `LexicalCandidateGenerator` as the sole lexical path; update `CandidateGeneratorTests`.
- Tests: `fuse eval ranking` on corpus v2 default arm unchanged within CI; grep test fails if
  `FtsCandidateGenerator` or `FUSE_FLAT_FTS` reappear; delete diagnostic-only tests that guarded
  only the retired class.
- Docs: `internals/scoping-internals.mdx`, `reference/configuration-keys.mdx` (remove row),
  `CHANGELOG.md`.
- Gate: `ranking.json` default configuration unchanged within CI on corpus v2; zero references
  in `src/` and `site/`.

## R4. Hook RPC verbose mode

Ambient verification hooks call `fuse host` over the pipe; `FuseHostClient` swallows RPC
failures so editors stay non-blocking.

- Work: add `FUSE_HOOK_VERBOSE=1` that logs RPC failures to stderr with method name and error
  code. Default remains silent. Implement on the hook client path and any shared RPC wrapper
  hooks use.
- Tests: unit tests on `FuseHostClient` (or hook wrapper) assert stderr lines for each failure
  class when verbose; assert zero stderr on default; test both enabled and disabled env parsing.
- Docs: `internals/host-rpc.mdx`, `reference/configuration-keys.mdx` (new row),
  `scenarios/ambient-verification.mdx`, `CHANGELOG.md`.
- Gate: default hook behavior unchanged (test proves silence); verbose mode covered for every
  swallowed failure site named in the item.

## R5. Narrow `fuse update` process termination

`ToolUpdateLauncher` currently kills broad `fuse` peer processes, which is risky on shared CI
boxes and multi-repo workflows.

- Work: terminate only the updating tool's own child lineage or processes holding the same
  install mutex; add `--force-kill-peers` for the old behavior with a changelog warning.
- Tests: unit test with mocked process list asserts the narrow kill set; integration test with
  two temp repo roots each running `fuse mcp serve` proves default update does not kill the
  unrelated peer; integration test proves `--force-kill-peers` restores old breadth (or document
  exact kill set in test name).
- Docs: `site/content/docs/start/install.mdx`, `reference/commands.mdx` (`fuse update` flags),
  `CHANGELOG.md`.
- Gate: both integration tests green; default path never kills cross-root serve (asserted).

## R6. Unified host/MCP/CLI contract doc

`host-rpc.mdx` gained security and overlay methods in 4.1, but integrators still learn three
parallel surfaces (nine MCP tools, CLI shortcuts, host RPC).

- Work: add `site/content/docs/reference/api-surfaces.mdx`: each user intent mapped to MCP tool,
  canonical CLI command, and host RPC method; note deprecated CLI aliases. Link from site nav.
- Tests: `Fuse.Cli.Tests` reflection test enumerates every `[JsonRpcMethod]` on
  `FuseHostService` and every `[McpServerTool]` on `FuseTools`; fails if any name is missing
  from the doc table (parse the MDX or a generated manifest committed beside the test).
- Docs: `api-surfaces.mdx`, links from `docs/index.mdx`, `connect-your-ai.mdx`,
  `reference/mcp-tools.mdx` (cross-link); `CHANGELOG.md` (new reference page).
- Gate: reflection/doc parity test green; manual review that all nine MCP tools and host RPC
  methods appear.

## R7. Host IPC hardening

Served-root binding shipped in 4.1. This item completes the threat model, served-root coverage,
and the Windows pipe ACL flag end to end.

- Work: document the local-trust threat model on `host-rpc.mdx` with explicit "any local user
  who can open the pipe can call RPC" language; implement `PipeSecurity` restricting to the
  current user when `FUSE_HOST_RESTRICT_PIPE=1`; reject RPC calls whose `root` differs from the
  daemon's served root on every RPC entry point (close any 4.1 gaps).
- Tests: served-root rejection on every RPC method (parameterized or one test per method);
  Windows-only test for `FUSE_HOST_RESTRICT_PIPE=1` pipe ACL (skip on non-Windows with
  `RequiresSdk` or platform trait, but must run in the SDK CI leg); negative test that default
  (flag off) behavior is unchanged on all platforms.
- Docs: `internals/host-rpc.mdx`, `reference/configuration-keys.mdx`, `SECURITY.md` local-trust
  subsection, `CHANGELOG.md`.
- Gate: served-root suite green; Windows ACL test green in CI; docs name limits and the flag.

## R8. Profile index hot path

`WorkspaceIndexStore` and `SemanticIndexer` sit on every warm read. Before R9 splits the store,
capture evidence on a large repo.

- Work: add a recorded profile run on NodaTime (or eShopOnWeb semantic tier): warm localize,
  symbol lookup, review planning, and single-file reconcile. Write numbers to
  `tests/benchmarks/results/profile-v42.json` with methodology notes. Identify top SQL/Roslyn
  frames for R9.
- Tests: `Fuse.Benchmarks` or `Fuse.Cli.Tests` validation test loads `profile-v42.json`,
  asserts required keys and schema version, and fails if the file is missing or stale relative
  to `Directory.Build.props` version; benchmark driver command documented in test name.
- Docs: `project/performance.mdx` (methodology pointer, not a headline claim); profile README
  in `tests/benchmarks/results/` if needed; progress log pastes top frames.
- Gate: artifact committed; validation test green; no product default changes.

## R9. Split `WorkspaceIndexStore`

The single ~1,600-line type owns schema migration, FTS search, symbol resolution, graph
edges, and sessions. This is the largest deferred item.

- Work: extract narrow types behind `IWorkspaceIndexStore` facade ports:
  `IndexSchemaMigrator`, `FtsSearchEngine`, `SymbolGraphStore`, `SessionStore`. Bump
  `WorkspaceIndexSchema.TargetVersion` only if on-disk layout changes. Use R8 profile to order
  extractions. The facade remains the only public entry for host and MCP.
- Tests: all existing `Fuse.Indexing.Tests` and retrieval tests green; new facade unit tests
  per port (mock dependencies, no full SQLite where the port is pure); one integration test per
  port that hits real SQLite; grep fails if `WorkspaceIndexStore` grows new responsibilities
  without going through a port.
- Docs: `internals/caching-internals.mdx` (module diagram and port list), `CHANGELOG.md` if
  schema bumps.
- Gate: zero benchmark regression on `performance.json` warm paths within CI noise; test count
  rises by at least one test per extracted port.

## R10. Unify scoping stacks

Fusion and Retrieval still carry separate `ContextPlan` types and centrality implementations
(PageRank on regex graph vs degree on semantic graph). 4.1 removed orphaned Fusion query knobs
but not the dual stack.

- Work: introduce a shared scoping module (extend `Fuse.Retrieval` or add `Fuse.Scoping`) used
  by MCP localize/review and by offline `fuse context` / fusion emission where overlap exists.
  Delete duplicate centrality; one `ContextPlan` public type. Migration: changelog names any
  CLI output shape change.
- Tests: Suite B review and Suite C localize unchanged on corpus v2 defaults; fusion golden
  outputs unchanged for `fuse reduce` path; new unit tests on the shared scoping module;
  regression test that Fusion and MCP review use the same `ContextPlan` type (compile-time or
  reflection).
- Docs: `internals/scoping-internals.mdx`, `concepts/scoping.mdx`, `CHANGELOG.md` for any output
  shape change.
- Gate: `review.json` and `localize.json` default arms within recorded CI bands; fusion golden
  tests green; duplicate centrality implementation deleted (grep gate).

## R11. Semantic-tier provider seam

`Fuse.Semantics` still references `Fuse.Plugins.Languages.CSharp.Roslyn` at compile time. The
syntax tier is provider-driven; the typed graph is not.

- Work: define `ISemanticLanguageProvider` (or extend the existing seam sketch) with C# as
  the only implementation; move wiring analyzers behind the provider registration; no second
  language in 4.2 (F6 entry bar unchanged). `Fuse.Semantics` must not reference Roslyn plugin
  projects directly.
- Tests: Suite A semantics unchanged (24/24 on OrderingApp); fake provider test registers
  alongside C# without touching C# edges; project reference test fails if
  `Fuse.Semantics` references `Fuse.Plugins.Languages.CSharp.Roslyn` directly.
- Docs: `internals/extending/semantic-analyzer.mdx`, `AGENTS.md` design invariants,
  `CHANGELOG.md`.
- Gate: `fuse eval semantics` recall/precision 1.0 on OrderingApp; reference graph test green.

## R12. Host RPC outcome assertions

Host and MCP tests still lean on wiring smokes. Expand outcome checks.

- Work: add outcome tests for protocol version mismatch message, reconcile stamped header on
  storm path, `fuse_check` grade stamping over RPC, and served-root rejection (coordinate with
  R7 so no duplicate asserts). Remove wiring-only "method exists" smokes replaced by these.
- Tests: in `Fuse.Cli.Tests` host and MCP suites; each test fails when its asserted string or
  grade is removed; test count recorded in progress log.
- Docs: `internals/host-rpc.mdx` (outcome table per RPC), `internals/daemon.mdx` if daemon
  paths are covered, `CHANGELOG.md` only if user-visible messages change.
- Gate: test count rises by at least four outcome tests; each linked to a behavior in the
  docs table.

---

## R13. Promote shared daemon to default-on

G5 shipped the shared `fuse host` daemon (single-instance lock, idle shutdown, version
handshake, `RemoteResidentWorkspaceProvider`), but `FUSE_DAEMON` defaults off and
`fuse mcp install` does not set it. Dogfooding on the Fuse repo with multiple agent sessions
showed each `fuse mcp serve` holding its own process while indexing, which defeats the D13
intent even when the daemon code exists.

- Work: make `fuse mcp serve` delegate to the shared daemon by default (invert
  `DaemonOptIn()` to default-on; add `FUSE_DAEMON=0` opt-out). Wire `fuse mcp install` to
  emit the default in Cursor/Copilot/Claude configs (or omit the var when default-on).
  Update `daemon.mdx`, `configuration-keys.mdx`, `CHANGELOG.md`, and `AGENTS.md` (D13: daemon
  is no longer "baking"). Re-measure G5 RSS one-truth on NodaTime after the flip.
- Tests: MCP integration tests: serve default-on attaches to one daemon per root; second serve
  does not duplicate resident workspace; `FUSE_DAEMON=0` in-process fallback; `mcp install`
  output test (golden or snapshot) documents default; `DaemonOneTruthTests` extended for
  default-on path; G5 RSS band re-recorded if materially changed.
- Docs: `internals/daemon.mdx`, `reference/configuration-keys.mdx`, `connect-your-ai.mdx`,
  `start/quickstart.mdx`, `AGENTS.md`, `CHANGELOG.md` (default flip + opt-out).
- Gate: all daemon tests green; install golden test green; changelog and config keys agree on
  default.

## R14. Index access coordinator and SQLite concurrency

Multiple `fuse mcp serve` processes (one per agent chat) plus a background semantic upgrade
each open `.fuse/fuse.db` and contend for writes. Observed failure: `SQLite Error 5: database
is locked` on `InitializeAsync` / `WriteMetaAsync`, surfaced to MCP clients as an opaque
`An error occurred invoking ...` and to CLI as an unhandled exception. WAL and `busy_timeout`
(30s) are insufficient when a writer holds a long transaction.

- Work: introduce `IndexCoordinator` in the MCP host: one writer queue per workspace root;
  cross-process contention returns `index_busy` instead of throwing. Chunk
  `UpgradeToSemanticAsync` commits per project or per N files. CLI and MCP share the
  coordinator API.
- Tests: concurrent MCP tool calls in one serve process do not deadlock; simulated lock returns
  `index_busy` string (pairs with R15); background upgrade does not block read-only open (R17);
  dogfood reproduction test (concurrent `fuse_find` during upgrade); stress test with N parallel
  opens; coordinator unit tests for queue ordering and single-writer invariant.
- Docs: `internals/caching-internals.mdx`, `internals/daemon.mdx`, `reference/mcp-tools.mdx`
  (error prefix), `CHANGELOG.md`.
- Gate: dogfood test green; zero unhandled `SqliteException` in coordinator tests; three gates
  green.

## R15. MCP and CLI operational error taxonomy

`FuseTools` remarks say tools return descriptive strings rather than throwing; in practice
`SqliteException` and other failures during `OpenIndexedAsync` escape as unhandled exceptions
and Cursor reports a generic MCP error. CLI `fuse find` crashes with a stack trace on lock.

- Work: wrap every `[McpServerTool]` entry point in a shared handler that catches expected
  operational failures (`SqliteException` busy/locked, `IOException`, `OperationCanceledException`)
  and returns stable prefixes: `index_busy:`, `index_not_built:`, `workspace_not_found:`,
  `validation_error:`, `internal_error:` (unknown). Mirror the same messages in CLI commands
  that open the store (exit 1, one line to stderr, no stack trace for expected cases).
- Tests: unit test per error prefix; MCP integration test per tool entry point asserts no throw
  on operational failures; CLI tests for `find`, `index`, `workspace status` assert exit code and
  stderr line; negative test that unknown exceptions still become `internal_error:` not a bare
  throw across MCP boundary.
- Docs: `reference/mcp-tools.mdx` (error prefixes table with retry guidance for agents),
  `reference/commands.mdx`, `CHANGELOG.md`.
- Gate: grep for unhandled `SqliteException` escape in MCP tool paths is zero; CLI operational
  test suite green; docs table matches code prefixes exactly.

## R16. Fast `fuse_workspace` status without blocking index

`fuse_workspace action=status` currently goes through `OpenIndexedAsync`, which on a clean
`.fuse` triggers a full syntax-first index and may block for minutes on a large repo. Status
should be the fastest tool: answer index mode, freshness, daemon PID, and build state without
waiting for semantic upgrade.

- Work: add a read-only fast path for `action=status` (and `action=doctor` summary header):
  if `.fuse/fuse.db` exists, open read-only, read `index_meta` (mode, `semantic_pending`,
  `stale_dirty_count`, file count), report daemon stats, return immediately. If no DB,
  return `not_indexed` without starting a build. Keep `action=index` as the explicit build
  path.
- Tests: integration test: status completes under 2s while background upgrade runs (mock or
  NodaTime fixture); cold repo without `.fuse` does not create DB; `action=index` still builds;
  doctor summary header uses fast path; MCP and CLI parity test same output shape.
- Docs: `reference/mcp-tools.mdx`, `reference/commands.mdx`, `CHANGELOG.md`.
- Gate: timed integration test green; behavior change named in changelog.

## R17. Read-only store open for warm reads

Every `WorkspaceIndexStore.InitializeAsync` writes FTS availability meta, which turns warm
reads into writes and fails under contention even when 3,000+ files are already indexed.

- Work: split `InitializeAsync` into write init (migrations, first create) and
  `OpenForReadAsync` (schema verify, no meta write) for warm opens. Foreground read tools use
  read open when the DB exists and schema version matches; write init only on create, migrate,
  or explicit `fuse index`. Pair with R14 single-writer discipline.
- Tests: concurrent read opens during background upgrade succeed on populated DB; first-create
  path writes meta once; `OpenForReadAsync` never calls `WriteMetaAsync`; regression test that
  warm read does not increment write counters (or uses a write spy); all `Fuse.Indexing.Tests`
  green.
- Docs: `internals/caching-internals.mdx`, `CHANGELOG.md` if open semantics change.
- Gate: concurrent-read integration test green; indexing unit tests green.

## R18. Decouple compiler tools from mandatory index open

`fuse_check` calls `OpenIndexedAsync` before attempting oracle or build-grade verification,
so verification is blocked by index contention even when `dotnet build` could answer.
`fuse_refactor` already loads the solution without the store.

- Work: reorder `FuseCheckAsync` to discover projects and run verify first; open the store only
  for repair-packet enrichment when available. Audit `fuse_test` for the same pattern. Add tool
  tier documentation.
- Tests: `fuse_check` build-grade when store is locked (mock) on buildable fixture; repair
  packets omitted with named note when store unavailable; checkgate suite unchanged; `fuse_test`
  covering-test path tested with store busy; MCP tests for both paths.
- Docs: `reference/mcp-tools.mdx` (tool tiers table: offline / compiler / indexed),
  `concepts/glossary.mdx` if terms added, `CHANGELOG.md`.
- Gate: checkgate honesty suite unchanged; new store-busy tests green; tiers table in docs matches
  code.

## R19. Daemon-owned index writes (G5 phase 2)

Complete D13 for the index: one daemon per root owns store writes; `mcp serve` is a stdio
adapter for store-backed tools when the daemon is active. Daemon-less CLI remains for CI.

- Work: move index open, reconcile, syntax-first, and semantic upgrade into `fuse host`;
  expose read/query RPCs for store-backed MCP tools; `mcp serve` delegates those calls over the
  pipe. Bump `FuseHostService.ProtocolVersion`. Daemon-less `fuse index` / `fuse find` unchanged.
- Tests: `DaemonOneTruthTests` extended for index state; two pipe clients see identical index
  answers; only one writer touches `fuse.db` during upgrade (file lock or write-audit test);
  version mismatch refuses cleanly; multi-session dogfood test (two serve + CLI find); RPC
  contract tests per new method; fallback when daemon down uses coordinator (R14) not raw open.
- Docs: `internals/daemon.mdx` (index ownership), `reference/api-surfaces.mdx` (RPC matrix),
  `internals/host-rpc.mdx`, `CHANGELOG.md`, `AGENTS.md` (single-writer invariant).
- Gate: multi-session dogfood test green without unhandled exceptions; protocol version bumped;
  all new RPC methods in doc parity test (R6).

## R20. Index-build progress in availability headers

Agents have no signal during a long cold index except a hung tool call. `semantic_pending`
exists in meta but is not surfaced early enough.

- Work: extend `OracleAvailabilityHeaderAsync` (and fast status, R16) to always lead with
  `index_state:` (`not_indexed`, `building_syntax`, `upgrade_pending`, `ready`, `index_busy`,
  `stale_as_of`) plus `files_indexed` when known. Return this header as the tool result body
  when a read tool cannot proceed yet, instead of throwing or hanging.
- Tests: MCP tests assert header shape for each `index_state` value (`not_indexed`,
  `building_syntax`, `upgrade_pending`, `ready`, `index_busy`, `stale_as_of`); test that blocked
  read returns header as body within bounded time; CLI review/find prepend same header; golden
  snapshots for header format.
- Docs: `reference/mcp-tools.mdx` (availability header spec), `concepts/claim-grades.mdx`,
  `CHANGELOG.md`.
- Gate: parameterized header tests green; no tool hangs without returning a status line in
  integration tests.

---

## R21. Agent-first zero-config defaults and self-healing store

The MCP path should need no tuning. Today humans must know about `FUSE_DAEMON`,
`FUSE_AUTO_UPDATE`, and manual `.fuse` deletion; corrupt `fuse-cache.db` self-heals but corrupt
`fuse.db` does not (F-001 refused index delete in cache recovery; `operator.mdx` overclaims
auto-recreate for the index). Agents see generic MCP errors instead of a rebuild-in-progress
signal.

- Work:
  - **`fuse mcp install` bundles agent defaults**: daemon on (or default-on per R13), auto-update
    on, background upgrade on, build capture on. No env block required in client config unless
    the user opts out. Document opt-outs in one "advanced" subsection, not the quickstart.
  - **`FUSE_AUTO_UPDATE` default on for `mcp serve`**: apply after session exit without killing
    sibling sessions (existing `stopOtherHosts: false` behavior). Keep `FUSE_AUTO_UPDATE=0` escape.
  - **Index self-heal**: on `SqliteCorrupt` / `database is locked` / schema mismatch, rebuild
    `fuse.db` from source (same policy as cache: derived data only) with a single serialized
    recovery per root; return `index_rebuilding:` to MCP callers instead of throwing. Pair with
    R14 coordinator so recovery does not race concurrent opens.
  - **Version upgrade**: keep `FuseBuildInfo.IsCompatible` major.minor rebuild; ensure the MCP
    response names `index_rebuilding after upgrade to {version}` not a bare failure.
  - **Deprecation shims** (D14 forward policy): old MCP tool names already removed at v4; any
    future renames register a one-major-version shim with an actionable replacement string the
    agent can follow.
- Tests: corrupt `fuse.db` fixture triggers rebuild and successful subsequent `fuse find`;
  version-mismatch stamp triggers rebuild with `index_rebuilding:` MCP response; auto-update
  launches when default on (mock launcher); opt-out `FUSE_AUTO_UPDATE=0` respected; `mcp install`
  golden test for bundled defaults; concurrent recovery serialized (one rebuild); install
  integration test on clean machine path (no env block in generated config).
- Docs: `connect-your-ai.mdx` (agent-first), `start/quickstart.mdx`, `internals/operator.mdx`
  (correct corrupt recovery), `reference/configuration-keys.mdx`, `CHANGELOG.md`.
- Gate: full self-heal test suite green; dogfood script automated in `Fuse.Cli.Tests`; docs
  match implemented recovery (operator page no longer overclaims).

## Sequencing

1. **R15, R16, R21 (self-heal + install defaults)** first: agent-visible reliability without
   waiting for R19.
2. **R14, R17** next: SQLite concurrency and read-only open; fixes the multi-session lock
   without waiting for full daemon consolidation.
3. **R13** after R14/R15: promote daemon default-on once contention returns named errors
   instead of opaque failures (daemon default alone does not fix index locking).
4. **R18, R20** in parallel with R14: compiler decoupling and progress headers improve the
   experience while index work lands.
5. **R19** after R13-R17: daemon-owned index is the D13 end-state; needs the coordinator and
   read-only paths first.
6. **R1, R2, R3** (original wave): CI honesty, planner doc truth, dead-code removal.
7. **R4, R5, R6, R7** next: operator and integrator experience without large refactors.
8. **R8** before **R9**: profile informs the store split.
9. **R9** before **R10**: narrowing the store makes scoping unification safer.
10. **R11** when a second semantic language is on the horizon; otherwise ship the seam with C#
    only and no new language.
11. **R12** in parallel with any host-touching item.

## Out of scope for V4.2

These review items are closed or belong elsewhere:

- F-001 through F-004, F-006, F-009, F-010, F-013, F-014, F-016, F-017, F-019, F-021,
  F-024, F-025, F-028, F-029 (shipped or answered in 4.1).
- CLI shortcut removal without deprecation shims (waits for v5 per D14 forward policy).
- OpenTelemetry export (F-017 landed metrics only; OTEL is a separate decision).
- Multi-language semantic tier beyond the seam (F6 / D10 entry bar).
- Items still on [v4-plan.md](v4-plan.md) Master checklist (B3 public benchmark page, frontier
  waves): not duplicated here unless a maintainer explicitly pulls them into 4.2.

## Honest ceilings

These are scope limits, not permission to ship partial items:

- R10 and R11 are architecture items; benchmark lift is not the gate; completeness and tests
  are the gate.
- R9 refactor may not move `performance.json` much; the gate is tests and zero regression, not
  a latency headline.
- R7 pipe ACL runs on Windows CI only; non-Windows must still pass served-root and default-path
  tests (platform skip is allowed only for the ACL assertion itself).
- R13 and R19 are sequential checklist rows, each must pass its own gate fully. R13 does not
  excuse incomplete index sharing; R19 delivers it.

## Reminders and conventions

- One item at a time; an item is not done until engine, tests, and docs are all landed.
- Three gates every item (`dotnet build`, `dotnet test --no-build`, `dotnet format
  --verify-no-changes`).
- Host RPC DTO or `[JsonRpcMethod]` changes bump `FuseHostService.ProtocolVersion`.
- Index schema changes bump `WorkspaceIndexSchema.TargetVersion` or ride `fuse_version` stamp.
- Append a progress log entry per item; **Tests** and **Docs** sections are mandatory fields.
- Never fabricate benchmark numbers; quote only `tests/benchmarks/results`.
- A green gate that ran zero new tests is not coverage (AGENTS.md change-safety invariant).

## Opt-in defaults (reference for promotion decisions)

Shipping defaults today (`configuration-keys.mdx`). Items marked **promote in 4.2** have an
active R item; others stay opt-in unless a gate records a lift.

| Variable | Default | Role | 4.2 action |
|----------|---------|------|------------|
| `FUSE_DAEMON` | off | Shared `fuse host` per repo | **R13: default on** |
| `FUSE_RESIDENT` | off | In-process resident workspace | Redundant when daemon on (daemon spawns with `FUSE_RESIDENT=1`); document, do not double-promote |
| `FUSE_BG_UPGRADE` | on | Syntax-first + background semantic | Keep on; pair with R14 chunked commits |
| `FUSE_BUILD_CAPTURE` | on | Oracle-grade check substrate | Keep on |
| `FUSE_QUERY_EXPANSION` | on | PRF lexical expansion | Keep on (ranking gate) |
| `FUSE_BUDGET_EXPANSION` | on | Budget-aware graph expansion | Keep on |
| `FUSE_DOWNGRADE_DROP` | on | Sketch before drop under budget | Keep on |
| `FUSE_CENTRALITY_WEIGHT` | 0.15 | Graph-centrality prior | Keep on (ranking gate) |
| `FUSE_UPDATE_CHECK` | on | NuGet update nag (stderr) | Keep on |
| `FUSE_METRICS` | off | `System.Diagnostics.Metrics` | Stay off (F-017); no OTEL |
| `FUSE_AUTO_UPDATE` | off | Auto-run `fuse update` after session | **R21: default on for mcp serve** |
| `FUSE_GIT_CHURN_WEIGHT` | off | Git recency prior | Stay off (benchmark cannot validate) |
| `FUSE_SKETCH_HUGE` | off | Huge-file sketch substitution | Stay off (corpus-neutral) |
| `FUSE_MEMBER_LEVEL` | off | Per-member retrieval | Stay off (latency cost) |
| `FUSE_PROJECT_GRAPH` | off | Cross-project `.csproj` expansion | Stay off (within noise on benchmark) |
| `FUSE_QUERY_REWRITE` | off | Rules-based query rewrite | Stay off (corpus-neutral) |
| `FUSE_FIELDED_COMMENTS` | off | Comments as BM25 field | Stay off (corpus-neutral) |
| `FUSE_FLAT_FTS` | off | Retired flat FTS generator | **R3: delete** |
| `FUSE_HOOK_VERBOSE` | off | Hook RPC failure logging | **R4: ship, stay off** |
| `FUSE_HOST_RESTRICT_PIPE` | off | Windows pipe ACL | **R7: ship complete with tests** |
| Git co-change prior | off | Ranking prior | Discharged (D6); diagnostic only |
| Dense embeddings | removed | K1 retired | Closed |

---

## Progress log

### 2026-07-14 Execution protocol tightened

Policy: every R item ships engine + tests + docs together; no deferral, no partial
implementation, no optional tests. Item bodies and gates updated; R6 doc parity test required;
R7 ACL implementation required; R5 two-root integration test required.

---

### 2026-07-14 Agent-first philosophy + R21

Product direction: Fuse is a silent agent surface; defaults and self-healing target MCP install,
not operator env tuning. Auto-update and corrupt-index rebuild promoted from "stay off" to R21.
Gaps noted: `fuse.db` lacks cache-style corrupt recovery; `operator.mdx` claims it does.

Gate: N/A (planning entry only).

### 2026-07-14 R13-R20 planned (dogfood MCP session)

Preconditions (Fuse repo, clean `.fuse`, fuse 4.1.0, multiple agent sessions): cold index
blocked `fuse_workspace` for 10+ minutes; index-backed MCP tools returned opaque
`An error occurred invoking ...`; CLI `fuse find` threw unhandled `SQLite Error 5: database is
locked` while PID 58552 held the DB during background semantic upgrade; `fuse_reduce` and
`fuse_refactor` worked (no store / no index on refactor path). G5 daemon code exists but
`FUSE_DAEMON` default off and does not fix index contention (resident only).

Shipped: roadmap items R13-R20 added; opt-in defaults table; sequencing updated.

Gate: N/A (planning entry only).

---

Append a timestamped entry per item as it lands.

---

### 2026-07-14 R1 CI split for RequiresSdk tests

Preconditions: `RequiresSdkIntegration` trait exists in `tests/Fuse.Cli.Tests/Host/RequiresSdkIntegration.cs`; default CI ran full suite including SDK-heavy tests that could skip vacuously.

Shipped: `.github/workflows/ci-sdk.yml` runs `Category=RequiresSdk` on win-x64 and linux-x64 with .NET 10; `.github/workflows/ci.yml` excludes them via `Category!=RequiresSdk` and runs `build/verify-ci-sdk.ps1` in the build job.

Commands: pending Group 11 (`dotnet build`, `dotnet test`, `dotnet format`, `./build/verify-ci-sdk.ps1`).

Tests: `build/verify-ci-sdk.ps1` (trait discovery, both CI legs present, win-x64/linux-x64 matrix); `RequiresSdkIntegration` skip helper; `DaemonOneTruthTests`, `ResidentWorkspaceTests` carry `Category=RequiresSdk`.

Docs: `site/content/docs/project/contributing.mdx` (two CI legs); `CHANGELOG.md` [4.2.0].

Deviations: none.

Gate: both CI legs green on PR touching RequiresSdk test; default leg excludes them; contributing page lists both commands -> pending Group 11 verify.

---

### 2026-07-14 R2 Finish briefing.md drift cleanup

Preconditions: `briefing.md` opening refreshed in 4.1; body still referenced fourteen tools, `ext/vscode/`, and stale benchmark figures.

Shipped: `briefing.md` swept for D15/U1 drift; `build/verify-briefing.ps1` fails on forbidden patterns; wired into `.github/workflows/ci.yml` build job; `roadmap/README.md` notes briefing tracks shipped product.

Commands: pending Group 11 (`./build/verify-briefing.ps1`).

Tests: `build/verify-briefing.ps1` (fourteen tools, ext/vscode, protocol.ts, stale MCP tool names).

Docs: `briefing.md`; `roadmap/README.md`; `CHANGELOG.md` [4.2.0].

Deviations: none.

Gate: verification script green; rg audit clean -> pending Group 11 verify.

---

### 2026-07-14 R3 Delete FtsCandidateGenerator

Preconditions: 4.1 gated flat FTS behind `FUSE_FLAT_FTS=1`; shipping default was `LexicalCandidateGenerator` only.

Shipped: removed `FtsCandidateGenerator`, `FUSE_FLAT_FTS`, and all references; `LexicalCandidateGenerator` is the sole lexical path.

Commands: pending Group 11 (`fuse eval ranking` on corpus v2 default arm).

Tests: `tests/Fuse.Retrieval.Tests/RetiredFlatFtsRemovalTests.cs` (grep gate for retired symbols); `CandidateGeneratorTests` updated for lexical-only path.

Docs: `site/content/docs/internals/scoping-internals.mdx`; `site/content/docs/reference/configuration-keys.mdx` (row removed); `CHANGELOG.md` [4.2.0].

Deviations: none.

Gate: `ranking.json` default configuration unchanged within CI on corpus v2; zero references in `src/` and `site/` -> pending Group 11 verify.

---

### 2026-07-14 R4 Hook RPC verbose mode

Preconditions: `FuseHostClient` swallowed RPC failures on the hook path; no opt-in diagnostics.

Shipped: `FUSE_HOOK_VERBOSE=1` logs swallowed RPC failures to stderr with method name and error code; default remains silent (`FuseHostClientVerboseHook`).

Commands: pending Group 11.

Tests: `tests/Fuse.Cli.Tests/Host/FuseHostClientVerboseTests.cs` (stderr on verbose; silence on default; env parsing).

Docs: `site/content/docs/internals/host-rpc.mdx`; `site/content/docs/reference/configuration-keys.mdx`; `site/content/docs/scenarios/ambient-verification.mdx`; `CHANGELOG.md` [4.2.0].

Deviations: none.

Gate: default hook behavior unchanged; verbose mode covers every swallowed failure site -> pending Group 11 verify.

---

### 2026-07-14 R5 Narrow fuse update process termination

Preconditions: `ToolUpdateLauncher` killed broad `fuse` peer processes, risky on shared CI and multi-repo workflows.

Shipped: default terminates only the updating tool's lineage and same-install peers; `--force-kill-peers` restores pre-4.2 breadth.

Commands: pending Group 11.

Tests: `tests/Fuse.Cli.Tests/Services/ToolUpdateLauncherIntegrationTests.cs` (`Launch_NarrowStop_DoesNotKillUnrelatedMcpServePeer`, `Launch_ForceKillPeers_KillsUnrelatedMcpServePeer`).

Docs: `site/content/docs/start/install.mdx`; `site/content/docs/reference/commands.mdx`; `CHANGELOG.md` [4.2.0].

Deviations: none.

Gate: both integration tests green; default path never kills cross-root serve -> pending Group 11 verify.

---

### 2026-07-14 R6 Unified host/MCP/CLI contract doc

Preconditions: integrators learned three parallel surfaces without a single intent map.

Shipped: `site/content/docs/reference/api-surfaces.mdx` with intent map, deprecated CLI aliases, and surface registry table; linked from site nav.

Commands: pending Group 11.

Tests: `tests/Fuse.Cli.Tests/ApiSurfacesDocParityTests.cs` (every `[McpServerTool]` and `[JsonRpcMethod]` in doc registry).

Docs: `api-surfaces.mdx`; `site/content/docs/index.mdx`; `site/content/docs/start/connect-your-ai.mdx`; `site/content/docs/reference/mcp-tools.mdx`; `site/content/docs/reference/meta.json`; `CHANGELOG.md` [4.2.0].

Deviations: none.

Gate: reflection/doc parity test green; all nine MCP tools and host RPC methods in table -> pending Group 11 verify.

---

### 2026-07-14 R7 Host IPC hardening

Preconditions: 4.1 shipped served-root binding partially; `PipeSecurity` and per-method coverage gaps remained.

Shipped: local-trust threat model documented; `FUSE_HOST_RESTRICT_PIPE=1` restricts Windows pipe ACL to current user; served-root rejection on every root-bound RPC entry point.

Commands: pending Group 11 (SDK CI leg for Windows ACL test).

Tests: `tests/Fuse.Cli.Tests/Host/FuseHostServedRootTests.cs` (parameterized per root-bound method); `tests/Fuse.Cli.Tests/Host/FuseHostPipeSecurityTests.cs` (ACL when flag on; default unchanged).

Docs: `site/content/docs/internals/host-rpc.mdx`; `site/content/docs/reference/configuration-keys.mdx`; `SECURITY.md`; `CHANGELOG.md` [4.2.0].

Deviations: none.

Gate: served-root suite green; Windows ACL test green in SDK CI leg; docs name limits and flag -> pending Group 11 verify.

---

### 2026-07-14 R8 Profile index hot path

Preconditions: `WorkspaceIndexStore` and `SemanticIndexer` on every warm read; no recorded profile before store split.

Shipped: `tests/benchmarks/results/profile-v42.json` committed with schema version, operation keys, and placeholder methodology notes for NodaTime; `ProfileV42ResultTests` validates artifact shape.

Commands: pending Group 11 (`fuse eval profile-v42 --repo NodaTime` for full numbers).

Tests: `tests/benchmarks/Fuse.Benchmarks.Tests/ProfileV42ResultTests.cs` (required keys, version alignment).

Docs: `site/content/docs/project/performance.mdx`; progress log top frames in artifact `notes`; `CHANGELOG.md` [4.2.0].

Deviations: artifact ships with `placeholder: true` until a provisioned profile run fills timings; no product default changes.

Gate: artifact committed; validation test green; no product default changes -> pending Group 11 verify.

---

### 2026-07-14 R9 Split WorkspaceIndexStore

Preconditions: single ~1,600-line type owned schema, FTS, symbol graph, and sessions.

Shipped: extracted `IndexSchemaMigrator`, `FtsSearchEngine`, `SymbolGraphStore`, `SessionStore` behind `WorkspaceIndexStore` facade; no on-disk schema bump.

Commands: pending Group 11 (`performance.json` warm paths within CI noise).

Tests: `WorkspaceIndexStoreBloatGateTests` (facade grep gate); `IndexSchemaMigratorIntegrationTests`, `FtsSearchEngineUnitTests`, `SymbolGraphStoreIntegrationTests`, `SessionStoreUnitTests`; all existing `Fuse.Indexing.Tests` green.

Docs: `site/content/docs/internals/caching-internals.mdx` (module diagram and port list); `CHANGELOG.md` [4.2.0].

Deviations: none.

Gate: zero benchmark regression on `performance.json` warm paths within CI noise; test count rises per port -> pending Group 11 verify.

---

### 2026-07-14 R10 Unify scoping stacks

Preconditions: Fusion and Retrieval carried separate `ContextPlan` types and duplicate centrality implementations.

Shipped: shared `Fuse.Scoping` module; single `ContextPlan` public type; duplicate centrality deleted; Fusion `ContextPlanBuilder` delegates to shared module.

Commands: pending Group 11 (`fuse eval review` and `fuse eval localize` on corpus v2 defaults).

Tests: `tests/Fuse.Fusion.Tests/Scoping/ContextPlanTypeUnificationTests.cs` (Fusion and MCP review share `Fuse.Retrieval.ContextPlan`); fusion golden outputs unchanged.

Docs: `site/content/docs/internals/scoping-internals.mdx`; `site/content/docs/concepts/scoping.mdx`; `CHANGELOG.md` [4.2.0].

Deviations: none.

Gate: `review.json` and `localize.json` default arms within recorded CI bands; duplicate centrality deleted -> pending Group 11 verify.

---

### 2026-07-14 R11 Semantic-tier provider seam

Preconditions: `Fuse.Semantics` referenced `Fuse.Plugins.Languages.CSharp.Roslyn` at compile time; syntax tier was provider-driven but typed graph was not.

Shipped: `ISemanticLanguageProvider`, `SemanticLanguageProviderRegistry`, `CSharpSemanticLanguageProvider` registered from Roslyn plugin; `Fuse.Semantics` has no direct Roslyn project reference.

Commands: pending Group 11 (`fuse eval semantics` on OrderingApp).

Tests: `tests/Fuse.Semantics.Tests/ProjectReferenceTests.cs` (no Roslyn csproj ref); `tests/Fuse.Semantics.Tests/SemanticLanguageProviderSeamTests.cs` (fake provider alongside C#); Suite A wiring unchanged.

Docs: `site/content/docs/internals/extending/semantic-analyzer.mdx`; `AGENTS.md` design invariants; `CHANGELOG.md` [4.2.0].

Deviations: none.

Gate: `fuse eval semantics` recall/precision 1.0 on OrderingApp; reference graph test green -> pending Group 11 verify.

---

### 2026-07-14 R12 Host RPC outcome assertions

Preconditions: host and MCP tests leaned on wiring smokes without outcome checks.

Shipped: outcome tests for protocol mismatch, served-root rejection, reconcile stamped headers, and check grade stamping; wiring-only smokes replaced where covered.

Commands: pending Group 11.

Tests: `tests/Fuse.Cli.Tests/Host/FuseHostRpcOutcomeTests.cs`; `tests/Fuse.Cli.Tests/Mcp/HostRpcOutcomeTests.cs` (at least four outcome tests; count recorded in Group 11 run).

Docs: `site/content/docs/internals/host-rpc.mdx` (outcome table); `site/content/docs/internals/daemon.mdx`; `CHANGELOG.md` [4.2.0] only where messages changed.

Deviations: none.

Gate: test count rises by at least four outcome tests; each linked to docs table -> pending Group 11 verify.

---

### 2026-07-14 R13 Promote shared daemon to default-on

Preconditions: G5 daemon code existed but `FUSE_DAEMON` defaulted off; each `fuse mcp serve` held its own process during indexing.

Shipped: `McpServeCommand.IsDaemonEnabled()` defaults true; `FUSE_DAEMON=0` opts out; `fuse mcp install` emits command-only config without env block.

Commands: pending Group 11 (`DaemonOneTruthTests` in SDK CI leg).

Tests: `tests/Fuse.Cli.Tests/AgentDefaultsTests.cs` (daemon default on; opt-out parsing); `tests/Fuse.Cli.Tests/Host/DaemonOneTruthTests.cs` (two clients, one truth); `tests/Fuse.Cli.Tests/McpInstallGoldenTests.cs` (no env block).

Docs: `site/content/docs/internals/daemon.mdx`; `site/content/docs/reference/configuration-keys.mdx`; `site/content/docs/start/connect-your-ai.mdx`; `site/content/docs/start/quickstart.mdx`; `AGENTS.md`; `CHANGELOG.md` [4.2.0].

Deviations: none.

Gate: all daemon tests green; install golden test green; changelog and config keys agree on default -> pending Group 11 verify.

---

### 2026-07-14 R14 Index access coordinator and SQLite concurrency

Preconditions: multiple `fuse mcp serve` processes plus background semantic upgrade contended on `.fuse/fuse.db`; `SQLite Error 5` surfaced as opaque MCP failures.

Shipped: `IndexCoordinator` with one writer queue per root; cross-process contention returns `index_busy:`; chunked semantic upgrade commits.

Commands: pending Group 11.

Tests: `tests/Fuse.Cli.Tests/Mcp/IndexCoordinatorTests.cs` (queue ordering, concurrent reads during upgrade, simulated lock returns `index_busy`).

Docs: `site/content/docs/internals/caching-internals.mdx`; `site/content/docs/internals/daemon.mdx`; `site/content/docs/reference/mcp-tools.mdx`; `CHANGELOG.md` [4.2.0].

Deviations: none.

Gate: dogfood reproduction test green; zero unhandled `SqliteException` in coordinator tests -> pending Group 11 verify.

---

### 2026-07-14 R15 MCP and CLI operational error taxonomy

Preconditions: `SqliteException` and other operational failures escaped MCP boundaries; CLI crashed with stack traces on lock.

Shipped: `FuseOperationalErrors` with stable prefixes; `ExecuteMcpAsync` wrapper on tool entry points; CLI commands mirror prefixes (exit 1, one stderr line).

Commands: pending Group 11.

Tests: `tests/Fuse.Cli.Tests/Mcp/FuseOperationalErrorsTests.cs` (unit per prefix); `tests/Fuse.Cli.Tests/Mcp/McpOperationalErrorBoundaryTests.cs` (no throw on operational failures); `FastWorkspaceStatusTests` CLI parity section.

Docs: `site/content/docs/reference/mcp-tools.mdx` (error prefixes table); `site/content/docs/reference/commands.mdx`; `CHANGELOG.md` [4.2.0].

Deviations: none.

Gate: grep for unhandled `SqliteException` escape in MCP tool paths is zero; CLI operational test suite green -> pending Group 11 verify.

---

### 2026-07-14 R16 Fast fuse_workspace status without blocking index

Preconditions: `fuse_workspace action=status` went through `OpenIndexedAsync`, blocking minutes on cold or upgrading workspaces.

Shipped: fast read-only path reads `index_meta` when DB exists; cold repo returns `not_indexed` without creating DB; `action=index` remains explicit build path.

Commands: pending Group 11.

Tests: `tests/Fuse.Cli.Tests/Mcp/FastWorkspaceStatusTests.cs` (cold no DB create; warm under 2s; doctor summary header; MCP/CLI parity).

Docs: `site/content/docs/reference/mcp-tools.mdx`; `site/content/docs/reference/commands.mdx`; `CHANGELOG.md` [4.2.0].

Deviations: none.

Gate: timed integration test green; behavior change named in changelog -> pending Group 11 verify.

---

### 2026-07-14 R17 Read-only store open for warm reads

Preconditions: every `InitializeAsync` wrote FTS availability meta, turning warm reads into writes.

Shipped: `OpenForReadAsync` for schema verify without meta write; foreground read tools use read open on warm DB; write init only on create, migrate, or explicit `fuse index`.

Commands: pending Group 11.

Tests: `tests/Fuse.Indexing.Tests/WorkspaceIndexReadOpenTests.cs` (no `WriteMetaAsync` on read open; concurrent read opens succeed).

Docs: `site/content/docs/internals/caching-internals.mdx`; `CHANGELOG.md` [4.2.0].

Deviations: none.

Gate: concurrent-read integration test green; indexing unit tests green -> pending Group 11 verify.

---

### 2026-07-14 R18 Decouple compiler tools from mandatory index open

Preconditions: `fuse_check` called `OpenIndexedAsync` before verify, blocking on index contention when `dotnet build` could answer.

Shipped: `FuseCheckAsync` runs verify first; store open only for repair-packet enrichment when available; tool tiers documented.

Commands: pending Group 11 (`fuse eval checkgate` unchanged).

Tests: `tests/Fuse.Cli.Tests/Mcp/FuseCompilerToolsStoreDecouplingTests.cs` (build-grade when store locked; repair packets omitted with note).

Docs: `site/content/docs/reference/mcp-tools.mdx` (tool tiers table); `site/content/docs/concepts/glossary.mdx`; `CHANGELOG.md` [4.2.0].

Deviations: none.

Gate: checkgate honesty suite unchanged; new store-busy tests green; tiers table matches code -> pending Group 11 verify.

---

### 2026-07-14 R19 Daemon-owned index writes

Preconditions: D13 single-writer intent not complete; each `mcp serve` could open and write the store independently.

Shipped: index open, reconcile, syntax-first, and semantic upgrade in `fuse host`; `RemoteIndexAccessProvider` delegates store-backed MCP calls over pipe; `FuseHostService.ProtocolVersion` bumped to 8.

Commands: pending Group 11.

Tests: `tests/Fuse.Cli.Tests/Host/RemoteIndexAccessProviderTests.cs` (daemon write delegation; coordinator fallback when daemon down); `tests/Fuse.Cli.Tests/Host/FuseHostProtocolTests.cs` (version mismatch); `DaemonOneTruthTests` extended; `ApiSurfacesDocParityTests` includes new RPC methods.

Docs: `site/content/docs/internals/daemon.mdx`; `site/content/docs/reference/api-surfaces.mdx`; `site/content/docs/internals/host-rpc.mdx`; `CHANGELOG.md` [4.2.0]; `AGENTS.md`.

Deviations: none.

Gate: multi-session dogfood test green without unhandled exceptions; protocol version bumped; new RPC methods in doc parity test -> pending Group 11 verify.

---

### 2026-07-14 R20 Index-build progress in availability headers

Preconditions: agents had no signal during long cold index except hung tool calls.

Shipped: `OracleAvailabilityHeaderAsync` and fast status lead with `index_state:` plus `files_indexed`; blocked reads return header as tool body within bounded time.

Commands: pending Group 11.

Tests: `tests/Fuse.Cli.Tests/Mcp/IndexStateAvailabilityHeaderTests.cs` (parameterized per state; golden snapshots; blocked find within 2s).

Docs: `site/content/docs/reference/mcp-tools.mdx` (availability header spec); `site/content/docs/concepts/claim-grades.mdx`; `CHANGELOG.md` [4.2.0].

Deviations: none.

Gate: parameterized header tests green; no tool hangs without status line in integration tests -> pending Group 11 verify.

---

### 2026-07-14 R21 Agent-first zero-config defaults and self-healing store

Preconditions: MCP path required env tuning; corrupt `fuse.db` did not self-heal; `operator.mdx` overclaimed auto-recreate.

Shipped: `fuse mcp install` command-only config with agent defaults in binary; `FUSE_AUTO_UPDATE` default on for mcp serve; corrupt/version-mismatch `fuse.db` rebuilds with `index_rebuilding:` MCP response; serialized recovery per root.

Commands: pending Group 11.

Tests: `tests/Fuse.Cli.Tests/Mcp/IndexSelfHealTests.cs`; `tests/Fuse.Indexing.Tests/WorkspaceIndexCorruptRecoveryTests.cs`; `tests/Fuse.Cli.Tests/McpInstallGoldenTests.cs`; `tests/Fuse.Cli.Tests/AgentDefaultsTests.cs` (auto-update default).

Docs: `site/content/docs/start/connect-your-ai.mdx`; `site/content/docs/start/quickstart.mdx`; `site/content/docs/internals/operator.mdx`; `site/content/docs/reference/configuration-keys.mdx`; `CHANGELOG.md` [4.2.0].

Deviations: none.

Gate: full self-heal test suite green; install integration test on clean path; docs match implemented recovery -> pending Group 11 verify.

---

### 2026-07-14 G11 Verification pass and Group 10/11 stabilization

Preconditions: the uncommitted R14-R21 work had never been run green end to end. Findings recorded before editing:
- The Release build failed with `MSB3021`/`MSB3027` file-copy locks, not a compile error: two orphaned `fuse host` daemons (`fuse-update-integration` temp roots, pids 71556/60536) held `tests/Fuse.Cli.Tests/bin/Release/**` DLLs. Traced to `ToolUpdateLauncherIntegrationTests` leaking the `fuse mcp serve` peer and its spawned daemon (`using var peer` calls `Process.Dispose`, which does not kill).
- `dotnet test Fuse.slnx --filter "Category!=RequiresSdk"` had 44 `Fuse.Cli.Tests` failures plus 1 `Fuse.Fusion.Tests` failure. Dominant root cause (`Fuse.Cli.Tests`): non-git temp roots resolve the store to the shared machine-wide `~/.fuse/fuse.db` (`FuseStorePaths.ResolveStorePath` fallback), so ~25 tests hammered one real 32 MB DB -> `SQLite Error 5` (32 s busy-timeout), `index_busy`, `FileCount: 46`, `fuse.db in use` on teardown. Remainder: real product bugs and two R18/R20 gaps (below). The `Fuse.Fusion.Tests` failure (`DependencyGraphCentralityTests`) was a refactoring regression: `DependencyGraphCentrality.BuildOutEdges` seeded nodes with a no-op `TryGetValue`, dropping isolated files.

Shipped (product): read-tool store opens and the per-read reconcile use a short (1 s) SQLite `busy_timeout` (`WorkspaceIndexConnectionFactory`/`WorkspaceIndexStore`/`IndexCoordinator.ReadBusyTimeoutMilliseconds`), so a contended store surfaces the `index_busy` availability header in ~1-2 s instead of blocking 30 s (completes R18/R20); cold builds keep the long timeout. `FuseHostJsonContext` registers `StreamJsonRpc.Protocol.CommonErrorData` (RPC errors propagated instead of throwing at the transport). MCP `fuse_check` delta mode initializes the session-baseline store when the index is not built, matching the host RPC path. `FuseHostService` gained an optional resident-provider override (null = unchanged production behavior; lets an in-process test give the daemon its own provider). `DependencyGraphCentrality` seeds every declaring file as a node.

Shipped (test infra): `TestStoreIsolation` (`.git`-marker per fixture + `FUSE_USER_DATA`/`FUSE_BG_UPGRADE` at module load) rolled out to every store-backed test class; RPC-fixture teardowns `ClearPool` before delete; `McpInstallTests`/`IndexCommandParseTests` share a collection (DotMake `Cli.Parse` registry race); the two `Console.SetError` classes share a collection; coordinator/install mutex tests hold the named mutex on a dedicated thread (Windows mutexes are thread-affine); the integration tests serve in-process (`FUSE_DAEMON=0`) and kill the peer tree; `ForceKillPeers` skips when process command-line introspection is unavailable; protocol assertion 7->8, apply expects `validation_error`, availability-header goldens pin the resident static; the new pipe-security test carries `[SupportedOSPlatform("windows")]` (CA1416).

Commands:
- `dotnet build Fuse.slnx -c Release` -> Build succeeded, 0 errors.
- `dotnet test Fuse.slnx -c Release --no-build --filter "Category!=RequiresSdk"` -> `Passed! Failed: 0` across all 16 assemblies (Fuse.Cli.Tests 284/284, Fuse.Fusion.Tests 141/141, Fuse.Indexing.Tests 113/113, Fuse.Retrieval.Tests 140/140).
- `dotnet format Fuse.slnx --verify-no-changes` -> exit 0 (clean).
- `fuse eval semantics` -> recall 100.0% precision 100.0% F1 1.00, 24/24 edges, OrderingApp.
- `fuse eval profile-v42 --repo NodaTime` -> refreshed `results/profile-v42.json` (real numbers, `placeholder: false`, index mode semantic, 512 files); `ProfileV42ResultTests` green.
- `fuse eval performance --repo NodaTime` -> index mode semantic; warm localize P50 43.9 ms, review plan P50 122.9 ms, incremental re-index P50 27.5 ms.
- `fuse eval ranking --manifest tests/benchmarks/corpus-v2.json --dataset-file prs-v2.json` -> executed; 20 tasks (2 repos indexed).

Numbers: `semantics` recall/precision 1.0 (24/24, gate met). `profile-v42.json` refreshed (source of truth for R8). `performance`/`ranking` run to scratch only; canonical `results/performance.json` and `results/ranking.json` left unchanged (this sandbox is under load and holds 4 of ~24 corpus-v2 repos, so its numbers are not canonical).

Deviations: the full corpus-v2 `ranking` re-run is corpus-bound here (only NodaTime, Scrutor, Specification, eShopOnWeb checked out -> 20/69 tasks; the co-change delta even flips sign on the partial subset), so the "ranking.json default unchanged within CI on corpus v2" gate cannot be verified in this environment; this session's changes do not touch the retrieval ranking prior (`GraphCentralityPrior`), only Fusion-side `DependencyGraphCentrality`. `performance` latencies run high (sandbox under load); the suite already labels timings environment-dependent. `ForceKillPeers` is environment-limited (the sandbox denies the cross-process command-line read the launcher enumerates by) and skips cleanly there. `RequiresSdk` integration tests (DaemonOneTruth, pipe ACL) remain excluded from the default leg and run in the SDK CI leg. Behavior change (bounded read-open busy timeout, CommonErrorData, delta-mode init) recorded in `CHANGELOG.md` [4.2.0].

Gate: build + test (`Category!=RequiresSdk`) + format all green; `semantics` 1.0/1.0; `profile-v42.json` refreshed and validated -> PASS. Full corpus-v2 `ranking` and cross-machine `performance` baselines are compute/corpus-bound and deferred to a canonical run.
