---
title: Scoping
description: Narrow a fusion to the files a task needs by name, by git change, or by query, expanding through the dependency graph.
---

**Scoping** selects a subset of the codebase relevant to a task instead of fusing
everything. Each mode starts from a **seed** set of files and expands through the
dependency graph to a configurable depth, so you get the relevant neighborhood whole
rather than a single file in isolation.

There are three modes, and a single fusion uses at most one of them.

| Mode | Flag | Seeds on | Use when |
|------|------|----------|----------|
| Focus | `--focus` | A path, filename, type, or directory | You know the area by name |
| Changes | `--changed-since` | Files changed since a git ref, plus dependents | Reviewing a branch or pull request |
| Query | `--query` | The top-ranked files for a search query | You have a topic but not a file name |

## Focus: scope by name

```bash
fuse dotnet --directory ./src --focus OrderService --depth 2
```

The seed resolves in order: an exact relative path, then an exact filename, then the
files that define a type of that name, then a directory prefix. `--depth` (1 to 10,
default 1) sets how many hops out through the graph Fuse follows. Focus pulls both
what the seed depends on and the files that depend on it.

## Changes: scope by git diff

```bash
fuse dotnet --directory ./src --changed-since main --include-dependents
```

The seed is the set of files changed since a git ref, which can be a branch, a commit,
or a relative reference like `HEAD~5`. `--include-dependents` (on by default) adds the
first-degree dependents of each changed file, the code most likely to break. This mode
needs `git` on your PATH. It is the strongest mode by measurement: 88 percent change
recall on real merged pull requests.

## Query: scope by topic

```bash
fuse dotnet --directory ./src --query "discount calculation at checkout" --query-top 5 --depth 2
```

Query ranks files by relevance, seeds on the top ones (`--query-top`, default 10), and
expands through their dependencies. Ranking weights a file's declared type and member
names and its path above the body, so a query lands on the file that declares a
concept rather than one that merely mentions it.

Focus and query ranking also fold in a small importance prior: at equal relevance, a file
that more of the codebase depends on ranks earlier, so a central type surfaces ahead of a
peripheral one. The prior is conservative and set by `FUSE_CENTRALITY_WEIGHT`; set it to 0
to rank on relevance alone.

For C#, query ranking works at member granularity, not whole files. It scores each declared
member, then for a matched file emits a thin host-type skeleton: the members the query hit
stay in full and the rest of the type collapses to signatures. A focused question pulls in
the methods it needs plus the shell of their type, not every line of the file. A query by
type name still pulls the whole type in; only a query that lands on specific members trims
the rest. The fallback path (no precision tier) chunks more coarsely but behaves the same way.

## Spending the token budget

When a query or focus runs under `--max-tokens`, Fuse picks the entries to emit by their
real reduced token cost, not a byte estimate: it reduces the candidates, then fills the
budget by relevance-per-token so the most relevant members survive and the budget lands
close to full. The single most relevant entry is always kept, so a scoped run never emits
nothing.

## Why expand through the graph

A changed file rarely stands alone. The type it calls, and the test that calls it, are
part of the same task. Graph expansion brings that neighborhood in so the agent does
not have to discover it with more round-trips. Per-hop scoring decays with distance, so
the seed neighborhood ranks above distant files; under a token budget the reduction-aware
pack (above) then keeps the highest relevance-per-token entries and drops the rest.

## A note on accuracy

The default dependency graph is regex-based and best-effort: it can miss edges from
dynamic dispatch or reflection. For accurate edges and member-level scoping, the
[precision tier](/docs/concepts/precision-tier) swaps in Roslyn. The ranking and
traversal are detailed in [Scoping internals](/docs/internals/scoping-internals).

## Next

Put a mode to work: [Scope a pull request](/docs/scenarios/scope-a-pr) or
[Ask one question](/docs/scenarios/ask-one-question).
