---
title: Fuse documentation
description: Fuse is a Roslyn-backed semantic context engine for .NET. It keeps a warm index of a workspace and resolves what code actually runs, then hands an agent the exact context a task needs. Start here to install it, connect it to your agent, and learn how it works.
---

Fuse is a semantic context engine for .NET coding agents. It keeps a warm, persistent
Roslyn-backed index of a workspace and resolves what the code actually runs (which
implementation is injected for an interface, which handler processes a request, which
endpoint serves a route, what a git diff impacts), then hands the agent the exact context
a task needs, reduced to fit the window. It ships as a Model Context Protocol server
(`fuse mcp serve`) with eight tools for an agent, and as a .NET global tool (`fuse`) for
the terminal and CI.

These docs are organized so you can read top to bottom to learn, or jump to a
reference page for exact detail.

## Get Going

<Cards>
  <Card title="What is Fuse" href="/docs/start/what-is-fuse" description="The outcome, with a worked example and the benchmark figure." />
  <Card title="Quickstart" href="/docs/start/quickstart" description="Index a workspace, then localize, resolve, and review." />
  <Card title="Connect to your AI" href="/docs/start/connect-your-ai" description="Set up the MCP server for Claude Code, Cursor, or Copilot." />
  <Card title="Why Fuse" href="/docs/start/why-fuse" description="The problem, the landscape, and how Fuse compares." />
</Cards>

## The Map

- **[Start](/docs/start/what-is-fuse)**: what Fuse is, why it exists, install, the first run, MCP setup, and the VS Code extension.
- **[Scenarios](/docs/scenarios/cut-tokens-dotnet)**: task-shaped how-tos. Each is a job with one command and real output.
- **[Concepts](/docs/concepts/how-fuse-works)**: how the warm index works, scoping, render tiers, Roslyn vs regex analysis, and a glossary.
- **[Reference](/docs/reference/commands)**: every command, flag, MCP tool, reducer, template, tokenizer, and output field.
- **[Internals and Extending](/docs/internals/pipeline)**: the pipeline, capability model, scoping and caching internals, and the extension guides.
- **[Project](/docs/project/benchmarks)**: benchmarks, performance, roadmap, changelog, and contributing.

## Next

New to Fuse? Start with [What is Fuse](/docs/start/what-is-fuse).
