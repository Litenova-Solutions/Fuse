---
title: Commands
description: The Fuse commands, what each one does, and the options they accept.
---

Fuse commands index a workspace into a persistent Roslyn-backed graph and then query that graph: build the index, map it, localize a task, resolve wiring, plan and emit context, review a change, look things up, and report state. A further command (`fuse reduce`) and the server commands (`fuse init`, `fuse mcp install`, `fuse mcp serve`, `fuse host`) round out the surface. This page states what each command does, when to reach for it, and which option set applies.

This page is for engineers choosing a command and for maintainers who need the exact dispatch behavior.

## Purpose and scope

The index lives in a single SQLite file at `.fuse/fuse.db` inside the workspace (or `~/.fuse/fuse.db` outside a git repository). The read commands build the index on first use, so an explicit `fuse index` is optional. Running `fuse` with no command prints usage guidance.

## Summary

| Command | Does | Reads index | Writes index |
|---------|------|-------------|--------------|
| `fuse index [path]` | Build or refresh the semantic index | n/a | yes |
| `fuse map [path]` | Print a workspace map (symbols, routes, counts) | yes | first-use |
| `fuse localize [path]` | Rank candidate files/symbols for a task (no bodies) | yes | first-use |
| `fuse resolve [path]` | Resolve a service, request, route, config, or symbol | yes | first-use |
| `fuse context [path]` | Plan and emit context for seeds (source bodies) | yes | first-use |
| `fuse review [path]` | Review the semantic impact of a change (or `--handoff` for a PR packet) | yes | first-use |
| `fuse impact <symbol>` | Blast radius for a symbol (or a NuGet package-upgrade break set) | yes | first-use |
| `fuse test <symbol>` | Run the covering tests for a symbol, scoped | yes | first-use |
| `fuse find <query>` | Exact symbol, path, or text lookup | yes | first-use |
| `fuse diagnostics [path]` | Report index state | yes | no |
| `fuse doctor [path]` | Diagnose the semantic load tier and per-project downgrade reasons | no | no |
| `fuse up [path]` | Report the environment-remediation plan (remedies per failure, workable subset); `--apply` applies the install-free NU1507 overlay and re-attempts the load | no | no |
| `fuse reduce` | Compact a known set of files or content | no | no |
| `fuse init` | Write a configuration file | no | no |
| `fuse mcp install` | Register Fuse with an MCP client | no | no |
| `fuse mcp serve` | Run the MCP server | n/a | first-use |
| `fuse host` | Run the long-lived host service | n/a | yes |

## fuse index

Build or refresh the persistent semantic index for a workspace. Loads the workspace through MSBuild/Roslyn when possible and falls back to syntax-level indexing otherwise.

```text
fuse index ./src
```

## fuse map

Print a map of the indexed workspace: symbols, routes, and counts.

```text
fuse map ./src --detail all
```

## fuse localize

Localize a task to ranked candidate files and symbols, with reasons and token costs and no source bodies. The cheap first step; follow with `fuse context`.

```text
fuse localize ./src --task "billing endpoint"
```

Accepts `--task`, `--route`, `--symbol`, `--service`, `--request`, `--config`, `--changed-since`, and `--max-candidates`.

## fuse resolve

Deterministically resolve .NET wiring to its target. Supply one of the options.

```text
fuse resolve ./src --service IOrderService
fuse resolve ./src --request CreateOrderCommand
fuse resolve ./src --route "POST /api/orders/{id}"
fuse resolve ./src --config Orders
fuse resolve ./src --symbol OrderService
```

## fuse context

Plan and emit context for a set of seeds, with source bodies at mixed render tiers, a semantic manifest, and per-file provenance.

```text
fuse context ./src --seed IOrderService --max-tokens 25000 --format xml
```

Accepts `--seed` (symbol), `--route`, `--depth`, `--max-tokens`, `--format` (`xml`, `markdown`, `json`), and `--plan-only`.

## fuse review

Review the semantic impact of a change since a git base ref: the changed files, the blast radius, and the packed context.

```text
fuse review ./src --changed-since origin/main --max-tokens 25000
```

Accepts `--changed-since`, `--max-tokens`, `--include-tests`, `--format`, `--plan-only`, `--handoff`, and `--check-session`. With `--handoff` it prints a paste-ready PR packet (changed files, public API delta, compiler-gate status, residual risk) instead of the review context, and refuses while the `--check-session` has unresolved introduced errors.

## fuse impact

Blast radius for a symbol before you edit it: the callers, implementers, consumers, and referencing types a change would touch, from the persisted semantic graph. Reads the index; builds it on first use.

```text
fuse impact OrderService --path ./src
fuse impact --package System.Text.Json --from-version 4.7.2 --to-version 8.0.0
```

Accepts a `symbol` argument, `--path`, `--limit`, and the package-upgrade trio `--package`, `--from-version`, `--to-version` (which diffs two cached NuGet package versions' public API instead of a symbol's graph blast radius).

## fuse check, fuse gate

Ambient verification (see [Verify every edit automatically](/docs/scenarios/ambient-verification)). `fuse check --delta` prints the diagnostics the session's edits introduced or resolved since its baseline; `fuse gate` exits nonzero while the session has introduced unresolved errors. Both read the delta from the running host over its pipe and never run a build; with no host serving the workspace they exit 0 silently.

## fuse verify --ci-parity

Rehearse what CI will run (see [Rehearse CI before you push](/docs/scenarios/rehearse-ci)). `fuse verify --ci-parity` reads the repository's GitHub Actions workflows, prints the dotnet command sequence they run in CI order, and names the steps that cannot be rehearsed locally (a step needing a secret, a package push) so nothing is silently skipped. Add `--run` to execute the rehearsable steps locally through the same executor build-grade verification uses.

```text
fuse check ./src --delta --fast
fuse gate ./src
```

`fuse check` accepts `--delta`, `--session` (default `hook`), and `--fast`; `fuse gate` accepts `--session`. Wire them automatically with `fuse mcp install --with-hooks`.

## fuse test

Run the covering tests for a symbol (the tests that reach it via the persisted `tests` edges) at build grade, scoped by filter so the whole suite never runs. Reads the index; run `fuse index` first.

```text
fuse test OrderService --path ./src
```

Accepts `--path` and `--limit` (maximum covering test types). Reports per-test verdicts with the grade; selection-only when no `tests` edge reaches the symbol.

## fuse find

Exact lookup over the index.

```text
fuse find OrderService --kind symbol
```

Accepts `--path` and `--kind` (`symbol`, `path`, `text`, `all`).

## fuse diagnostics

Report the index state: schema version, mode, status, record counts, and the store location.

```text
fuse diagnostics ./src
```

## fuse doctor

Diagnose the semantic load and report the achieved tier and the concrete per-project reason for any downgrade. Where `fuse diagnostics` reports the state of an already-written index, `fuse doctor` actively loads the workspace through MSBuild and Roslyn and explains why the oracle tier was or was not reached, so you know whether the compiler-backed answers are available. It names each project as loaded, loaded with compile errors (graph-grade), or not loaded (unrestored, SDK mismatch, or a build error), and reports the overall tier: oracle-grade (every project loaded clean), graph-grade (partial), or syntax.

```text
fuse doctor ./src
```

## fuse up

Report the environment-remediation plan for a workspace: run the same load diagnosis as `fuse doctor`, classify each downgraded project's failure against a knowledge base of known environment problems (for example NU1507 central-package-management source mapping, NETSDK1045 SDK band, MSB4018 task load), and name the remedy that would address each plus the workable-subset line (for example "6 of 8 projects oracle-grade; blockers: NU1507 on 2"). Repository-code failures (an ambiguous type, a bad compiler option) are named as not fixable by `fuse up`, never edited. By default it is report-only. Add `--apply` to apply the install-free NU1507 overlay (write the overlay NuGet config to a temp file, run `dotnet restore --configfile <overlay>` never touching the repository, then re-attempt the load and print the new per-project tiers); the apply path is bounded to one round. The consent-gated SDK and workload installs are never run without `--allow-install`; without it they are reported, not applied.

```text
fuse up ./src
```

## fuse reduce

Compact a caller-supplied set of files or piped content, running reduction without walking a directory. See [Options reference](/docs/reference/options) for the reduction levels.

```text
fuse reduce --files src/OrderService.cs --level skeleton
```

## fuse init, fuse mcp, fuse host

`fuse init` writes a configuration file. `fuse mcp install` registers Fuse with an MCP client and `fuse mcp serve` runs the server (see [Tools reference](/docs/reference/mcp-tools)). `fuse host` runs the long-lived host service.
