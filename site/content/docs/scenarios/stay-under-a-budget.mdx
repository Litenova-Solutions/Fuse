---
title: Stay under a token budget
description: Cap a context plan with a hard token limit so the output fits a model's context window, with the must-keep seeds always retained.
---

**Goal:** keep a context plan within a fixed token budget so it fits the context window of
the model you are feeding.

## Cap with a Hard Limit

```bash
fuse context ./src --seed OrderService --max-tokens 25000
```

`--max-tokens` caps the plan at the limit. The must-keep seeds are always included; the
rest of the neighborhood fills the budget by weight-per-token, and Fuse chooses the render
tier of each file (full, compressed, or skeleton) so the budget lands close to full rather
than overshooting. A budget of 0 means no limit.

## Cap a Review the Same Way

```bash
fuse review ./src --changed-since origin/main --max-tokens 25000
```

`fuse review` honors the same budget. The changed files are always kept, so when the
budget is tight the cut falls on the periphery of the blast radius first, never on the diff
itself.

## What Survives the Cut

Because the plan fills by weight-per-token, the files closest to the seeds survive and the
least relevant drop. The single most relevant entry is always kept, so a budgeted call
never emits nothing. The manifest lists each emitted file and its token cost, so you can
see exactly what fit.

## Suggested Budgets

| Workflow | Command shape | Budget |
|----------|---------------|--------|
| Architecture view | `fuse context --seed ... --max-tokens` | 15k to 50k |
| Focused area | `fuse context --seed ...` | 15k to 50k |
| Change review | `fuse review --changed-since ...` | 15k to 50k |

## When to Use It

Set a budget whenever the plan is destined for a model with a fixed window or when you are
capping the cost of a run. The seeds (or the changed files, in a review) are always
retained, so the budget trims context the task needs least.

## Related Concept

How the plan spends a budget is covered in [Scoping](/docs/concepts/scoping); choosing the
smallest render tier that fits is in [Reduction levels](/docs/concepts/reduction-levels).
