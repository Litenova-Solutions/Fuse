---
title: Why Fuse, and how it compares
description: The problem agentic coding tools hit on large codebases, what generic packers and RAG indexers do and where they fall short, and what Fuse does differently.
---

Most tools that feed code to a language model fall into two camps: packers that dump
the repository as text, and retrieval indexes that return fuzzy chunks. Fuse is
neither. It is a structural, .NET-aware optimizer that cuts tokens while keeping the
public API intact. This page explains the problem, the landscape, and the trade-offs.

## The Problem: The Explore Phase

An agentic coding tool such as Claude Code, Cursor, or GitHub Copilot does not start
by editing. It starts by exploring. To make a safe change it first has to learn the
codebase: which files exist, which types matter, what calls what. So it lists
directories, runs searches, and opens files one at a time, reading each result into
its context window before deciding what to open next.

On a small project that is cheap. On a solution with hundreds or thousands of C#
files it is the expensive part of the task:

- **Token cost.** Every file the agent opens to orient itself consumes context window
  it could have spent on the actual change. Much of that content is comments, using
  directives, and boilerplate the agent does not need to reason about structure.
- **Round-trips.** Discovery is sequential. The agent reads, decides, reads again.
  Each hop is a model call with latency, and the agent often re-reads files it
  already saw because nothing kept a compact map of the codebase.
- **Lost structure.** Reading files in isolation, the agent has to reconstruct the
  dependency relationships, the public surface, and the endpoints by hand.

The measured shape of this is on the [Benchmarks](/docs/project/benchmarks) page
(Layer 4). Over 90 real merged pull requests, a blind agent must read each file a
change needs at least once, a lower bound of about seven round-trips, while one scoped
Fuse call acquires that context in a single response at about 37,000 tokens and 49
percent recall, against roughly 425,000 tokens for a generic-packer dump at the same
one call. The round-trip count is a bound from ground truth, not a measured agent;
the wall-clock benefit (an explore loop of minutes collapsing to one call) is a
theory-grounded illustration of the mechanism, labeled as such, not a benchmark.

## The Landscape

### Generic Repository Packers

Tools such as Repomix, Code2Prompt, and Gitingest concatenate a repository into one
text blob with a header and per-file separators. They are language-agnostic and easy
to run, which is their strength.

Their limit for code structure is that they treat source as plain text. They do not
know what a C# type or a method signature is, so they cannot reduce by structure,
cannot preserve the public API while dropping noise, and cannot scope to the files a
task touches. The envelope they add often makes the output larger than raw
concatenation: on the benchmark corpus, Repomix output measured 1.3 to 3.9 percent
larger than the raw files, while Fuse default was smaller than both. Because a packer
dumps the whole repository, both arms acquire a task's context in one call, but the
packer pays for everything: in the Layer 4 scenario, a Repomix dump averaged about
425,000 tokens against about 37,000 for one scoped Fuse call (at 49 percent recall of
the changed files). The packer's recall is 1.00 only because it includes the entire
repository.

### RAG and Embedding Indexers

Retrieval-augmented generation indexes a codebase into vector embeddings and returns
the chunks most similar to a query. This is useful for open-ended search across a
very large corpus and for natural-language questions.

Its limits for code are structural. Embeddings return ranked chunks, not whole types
or a faithful public surface, so recall is partial and the result is non-deterministic:
the same query can return different chunks as the index or model changes. A chunk
boundary can split a method from its signature. And the index has to be built and
kept fresh, which adds infrastructure. RAG answers "what looks similar to this
text"; it does not answer "give me this type and everything it depends on, intact".

## What Fuse Does Differently

State the value against the three things you would otherwise reach for, strongest first.

1. **Versus no tool, the baseline an agent lives in.** This is the headline. A blind
   agent must read each file a task touches at least once before it has the context: a
   measured structural lower bound of 6.9 files on average over 90 real merged pull
   requests, and up to 11.9 on a large library. Fuse acquires the same context in one
   scoped call. That is the round-trip collapse, and it is the value you feel.

2. **Versus a generic packer (Repomix).** Both acquire context in one call, so Fuse ties
   on round-trips and wins on tokens: about 37,000 tokens for the scoped set against about
   425,000 for the whole-repo dump, roughly 11 times fewer, because Fuse scopes and the
   packer dumps. To be plain: Fuse does not beat Repomix on round-trips; it ties at one
   call and wins on tokens.

3. **Versus RAG and embedding indexes.** Fuse returns whole types and their dependency
   neighborhood, deterministically, with no index to build or keep fresh, where a vector
   index returns ranked fuzzy chunks and can split a method from its signature.

4. **And the code it returns keeps the surface intact.** Default and `--level aggressive`
   reduction remove 7 to 46 percent of tokens while keeping 99 to 100 percent of public
   types and methods; skeleton mode removes 39 to 56 percent for an architecture map.

Fuse is not only one call. Across a task it is a few cheap scoped calls (survey with
`fuse_toc`, drill in with `fuse_focus` or `fuse_search`, review with `fuse_changes`)
instead of dozens of blind reads, and a `session` id means later calls do not resend
files already returned. A packer dumps once; it cannot refine. See
[The explore phase](/docs/concepts/the-explore-phase) and
[Sessions and deltas](/docs/concepts/sessions-and-deltas).

### The Choices Behind It

Fuse is built specifically for the explore phase on a .NET codebase, and it makes a
different set of choices:

- **Structural.** It parses C# enough to reduce by structure: remove comments,
  usings, namespaces, and whitespace, or drop method bodies to leave a signature-only
  **skeleton**. The output represents the same source in fewer tokens.
- **.NET-aware.** It knows project files, Razor, and configuration, excludes test
  and generated files by default, and can map ASP.NET routes and project references.
- **Fidelity-preserving.** Reduction is not deletion. An independent Roslyn oracle
  confirms the default and `--level aggressive` modes keep 99 to 100 percent of public types and
  methods, and `fuse verify` reports the preserved surface for any run.
- **Deterministic.** Same input, same output. No similarity ranking in the default
  path, no index drift.
- **Scoped, not fuzzy.** Scope a fusion to a type and its dependencies, the files a
  git diff touched, or the files a query ranks highest, expanded through a dependency
  graph, so the agent gets the relevant neighborhood whole rather than ranked chunks.
- **MCP-native.** `fuse mcp serve` is a Model Context Protocol server with nine tools,
  so the agent fetches scoped context in one call instead of reading files
  one by one.

## How It Compares

| Capability | Generic packers | RAG / embeddings | Fuse |
|------------|-----------------|------------------|------|
| Round-trips to acquire context | One dump | One query, partial | One scoped call, or a few cheap refining calls |
| Context per call | Whole repo | Ranked chunks | Scoped set, about 12.8x fewer tokens than a packer dump |
| Understands C# structure | No, plain text | No, opaque chunks | Yes, types and signatures |
| Cuts tokens vs raw | Often larger than raw | Per chunk only | 7 to 40 percent at full API fidelity |
| Keeps the whole public API | Only by including everything | No, partial recall | 99 to 100 percent, verified |
| Deterministic output | Yes | No, similarity-ranked | Yes |
| Scope to a task | No | By query only | Type, git change, or query, with graph expansion |
| Built for agents (MCP) | Rarely | Varies | Nine tools, built in |
| Setup and freshness | None | Build and refresh an index | None, runs on demand |

Fuse is complementary to the other two where their strengths lie. For a polyglot
repository a generic packer is simpler, and for open-ended semantic search across a
huge corpus an embedding index has its place. For giving a .NET agent the right code
with the API intact and the tokens cut, Fuse is purpose-built.

## Honest Boundaries

The C# dependency graph is syntax-level and best-effort: it can miss edges that
come from dynamic dispatch or reflection because types are not resolved across assemblies.
Query and focus scoping rest on a lexical index, so a query that
shares no vocabulary with the target file can miss. The
[Benchmarks](/docs/project/benchmarks) page reports exactly where Fuse ties or loses,
not only where it wins.

## Next

See the measured results on the [Benchmarks](/docs/project/benchmarks) page, or
[install Fuse](/docs/start/install) and try it on your own solution.
